/**
 * ブランドマーク（銀杏の葉）。人間の指示 2026-08-22。
 *
 * ── 画像ではなく図形で持つ ──────────────────────────────
 * `.png` を置くと、拡大表示・高解像度・ダークモードのぶんだけ枚数が要る。
 * 図形なら 1 つで足り、**バイト数も小さいので Worker の中に置ける**
 * （和文書体を静的アセットへ出した DECISIONS #256 の逆で、こちらは
 * 数百バイト）。ファビコンは同じ絵を `public/favicon.svg` に置く
 * （ブラウザのタブは HTML の中の図形を読めないため、そこだけ 2 つある）。
 *
 * ── 色を固定してある ────────────────────────────────────
 * 濃色の topbar と、白いブラウザのタブの両方に載る。**`currentColor` に
 * しない** — topbar の文字色（白）を継いでしまい、金の柄が消える。
 * 明色・暗色のどちらでも読める中間の緑を選んである。
 */
export function Logo({ className }: { className?: string }) {
  return (
    <svg
      viewBox="0 0 32 32"
      className={className ?? "pk-logo"}
      role="img"
      aria-label="ProofKeeping"
    >
      {/* 左葉。上の細い切れ込みから外へ開き、下は中央の軸へ向かって上がる。 */}
      <path
        fill="#6e9c7e"
        d="M15.4 4.6 A 12.6 11.4 0 0 0 3.6 21.2 C 6.2 23.4, 10.2 23.2, 13.2 20.6 L 13.2 15.4 A 2.8 2.8 0 0 1 15.4 12.7 Z"
      />
      {/* 右葉。**左葉を x=16 で鏡にしただけ。** 数値を変えるときは両方直す。 */}
      <path
        fill="#6e9c7e"
        d="M16.6 4.6 A 12.6 11.4 0 0 1 28.4 21.2 C 25.8 23.4, 21.8 23.2, 18.8 20.6 L 18.8 15.4 A 2.8 2.8 0 0 0 16.6 12.7 Z"
      />
      {/* 柄。金は `--accent2` と同系（ブランドの 2 色目）。 */}
      <path
        fill="none"
        stroke="#c09b4f"
        strokeWidth="2.5"
        strokeLinecap="round"
        d="M16 21.6 C 16.1 25.4, 17.4 27.6, 19.9 28.4"
      />
    </svg>
  );
}
