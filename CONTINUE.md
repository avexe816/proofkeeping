# CONTINUE

## 最終状態
- main HEAD: P5-01 のマージ待ち（PR は下記）
- 完了: **Phase 0〜4 と P5-01**（P4-08 を除く）
- 次: **P5-02（取引先マスタ）**

## 次にやること
1. `git fetch origin && git checkout main && git pull`
2. `docs/tasks/P5-02.md` を読む（依存 P5-01 は完了済み）
3. P5-02 → P5-03 → P5-04 の順に進める。3 つとも依存が直列なので
   同じバッチにまとめてよい（workflow.md §3）

### P5 の依存関係（`docs/tasks/P5-*.md` の `**依存**:` 行）
```
P5-01 ✅ → P5-02 → P5-03 → P5-04 ┬→ P5-05 → P5-12 → P5-13
                                  │           └→ P5-14 → P5-15
                                  └→ P5-06 → P5-07 ┬→ P5-08
                                                    ├→ P5-09
                                                    └→ P5-10
P5-01 → P5-11（検索・電帳法対応。独立して進められる）
```

## 申し送り

### すぐ効くもの
- **`packages/billing` は空に近い。** P5-03 / P5-04 が料金解決と税計算の
  純粋関数を置く場所。`fiscalYearOf()` / `documentNumber.ts` は既にある。
- 料金の解決順序は billing.md §8 の 5 段階（施設 + 客室タイプ + 作業種別 →
  … → 取引先の既定）。**該当が無いタスクを黙って除外しない。**
  `unitPrice = 0` の明細として計上し、画面に警告を出す（同 §8）。
- 税額は**税率ごとに 1 回だけ**端数処理する（PK-SPEC-P5 §2.5 MUST /
  billing.md §4）。明細行ごとに端数処理しない。`invoiceTaxSummary` が
  その「1 回」の置き場所で、`invoiceLine` に税額の列を持たせていない。
- 請求書の発行は §4.1 の 10 手順。**①〜⑥ が 1 トランザクション**で、
  ⑦以降は Queue（PDF → 送付）。P5-07 が実装する。分割して着手しない。

### P4 の積み残し（人間待ち）
- **P4-08 誤検知率の検証（人間が実施）。** これが通るまで動かせないもの:
  - R007 / R008 / R009 / R011（§3 に条件の記述が無い / OPEN_QUESTIONS #066）
  - 確信度の暫定値（DECISIONS #116）
  - W-25 の閾値入力欄（OPEN_QUESTIONS #068）
- P5 は P4-08 に技術的に依存しないので飛ばして進めた
  （workflow.md §2「`（人間が実施）` の task → 飛ばして次へ」）。

### 未解決の問い（新しい順）
- #068 ルール設定の画面から閾値を編集できない（engine が鍵を公開していない）
- #067 W-25 の画面番号が PK-SPEC-P1 と P4 で衝突
- #066 R007 / R008 / R009 / R011 に条件の記述が無い
- #065 W-07 の「確信度の内訳」「時系列」がプロトタイプにだけある
- #064 入室記録（`roomAccessLog`）を登録する画面が仕様に無い
- #063 抑制条件が参照する施設の 2 属性に対応する列が無い

### 直近の設計判断
- #121 請求の 8 表に施設スコープを掛けない（`NO_PROPERTY_SCOPE` を明示）
- #120 請求・領収の 6 表の entityPrefix
- #119 月次監査レポートは表を持たず R2 のキーだけで管理する
- #118 ルール設定の経路を `:ruleCode` にする
- #117 `engineVersion` を 1.1 へ
- #116 確信度の式が仕様に無いルールは根拠の強さを 1 つだけ加点する
