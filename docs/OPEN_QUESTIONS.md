# 未解決事項

Claude Code はここに追記して作業を止める。人間が回答したら「解決済」へ移す。

## 記入テンプレート

```
### #NNN タイトル
- 提起: YYYY-MM-DD / タスクID 実装中
- 内容: 何が判断できないか
- 影響: 影響するタスクID
- 暫定対応: とりあえずどうしたか（あれば）
```

---

## 未回答

### #002 最初に実接続する PMS
- 提起: 未着手 / P6-06
- 内容: 導入顧客が利用している PMS を調査してから決める。想定で作らない。
- 影響: P6-06

### #003 スマートロックの対象機種
- 提起: 未着手 / P6-08
- 内容: 顧客の導入予定機器に依存。
- 影響: P6-08

### #004 写真の既定保持期間
- 提起: 未着手 / P7-10
- 内容: 6 か月が顧客に受容されるか。清掃会社は請求根拠として 13 か月を求める可能性。
- 影響: P7-10, 課金プラン

### #005 1 ユーザーの複数組織所属
- 提起: 未着手 / P0-06
- 内容: 清掃会社の兼務者を想定。Membership の設計上は可能だが UI が未定義。
- 影響: P0-06, P0-14
- 補足（2026-08-11 / P0-06 実装中）: スキーマ上は **`user` を組織スコープにした。**
  組織は 16 シャードへ分散するため、1 行の `user` を複数組織で共有すると
  シャードをまたぐ参照が発生する。同一人物が 2 組織に所属する場合は
  組織ごとに `user` 行と `membership` 行を持つ（データは重複する）。
  この選択の副作用としてメールアドレスの一意性が組織内に閉じ、
  メールログインの経路が塞がる（#014）。UI は依然として未定義。

### #007 `SHARD_MAP` への書き込みと組織のシャード移送手順を持つ task が無い
- 提起: 2026-08-11 / P0-03 実装中
- 内容: `packages/db/src/router.ts` は `SHARD_MAP` を**読むだけ**で実装した。
  `docs/tasks/P0-03.md` の やること が 4 関数（`fnv1a32` / `resolveShard` /
  `getShardBinding` / `getTenantDb`）のみで、書き込みは含まれないため
  （CLAUDE.md §1-4「task に書かれていないことを実装しない」）。
  しかし現状、`shard:{organizationId}` を書く処理を持つ task が
  `docs/tasks/` の P0〜P7 137 件のどこにも無い。`docs/PK-SPEC-P7.md` §4.4
  （テナント移送）の手順 4「ルーティングテーブルを更新」がこれに当たると
  読めるが、対応する P7 の task には書き込みの記述が無い。
- 影響: P7-07（テナント移送）、および移送を要する運用全般。
  書き込みが無くても P0〜P6 は成立する（ハッシュのみで解決できる）ため、
  現時点では実害はない。
- 暫定対応: `router.ts` の冒頭コメントに、書き込みを実装する者が守る MUST
  （TTL 禁止・一括削除禁止・明示マッピング以外のキーを置かない・
  移送完了前に書く）を記載した。書き込み側は `0 <= idx < SHARD_COUNT` の
  検証を行う責任を負う（DECISIONS #007）。

### #008 `getTenantDb()` の同期・非同期が仕様書とルールで食い違う
- 提起: 2026-08-11 / P0-03 実装中
- 内容: `docs/PK-SPEC-P0.md` §19.3 のコード例は `shardIndexOf()` /
  `getShardBinding()` / `getTenantDb()` をすべて**同期**関数として書いており、
  明示マッピングの参照が無い。一方 `.claude/rules/architecture.md` §1 は
  `resolveShard()` が `env.SHARD_MAP.get()` を読む**非同期**関数として書いている。
- 影響: P0-03、および `getTenantDb()` を呼ぶすべてのリポジトリ関数（P0-07 以降）。
- 暫定対応: **非同期を採用した。** `docs/tasks/P0-03.md` の完了条件に
  「KV の明示マッピングがハッシュより優先される」があり、これは KV 読み取りを
  必須にするため、同期では実装できない。CLAUDE.md §7 に従い矛盾をここに報告する。
  結果として `getTenantDb()` は `Promise` を返し、呼び出し側は `await` が要る。
  `docs/PK-SPEC-P0.md` §19.3 のコード例の修正可否は人間の判断を待つ。

### #011 `role` の語彙が実装契約書と他のすべての文書で食い違う
- 提起: 2026-08-11 / P0-06 実装中
- 内容: `docs/PK-IMPL-CONTRACT.md` §2.10 / §4 の権限マトリクスは
  `CLEANER` / `SITE_LEAD` / `OPS_MANAGER` / `OWNER` / `VIEWER` / `PLATFORM_ADMIN`
  の 6 語で書かれている。一方 `.claude/rules/security.md` §1 と
  `docs/PK-SPEC-P0.md` §23.1、P1〜P6 の全仕様書は
  `OWNER` / `ORG_ADMIN` / `PROPERTY_MANAGER` / `INSPECTOR` / `CLEANER` /
  `VENDOR_ADMIN` / `AUDITOR` の 7 語で書かれている。
  CLAUDE.md §7 は「仕様書と矛盾したら実装契約書を優先」と定めるが、
  契約書の 6 語は §2.10 / §4 以外のどこにも現れず、対応表も無い。
  `SITE_LEAD` は `PROPERTY_MANAGER` か `INSPECTOR` か、`OPS_MANAGER` は
  `ORG_ADMIN` か、`VIEWER` は `AUDITOR` か、いずれも推測になる。
- 影響: P0-06（`membership.role`）、P0-10（権限マトリクス）、
  P0-13（越境テスト）、以降の全画面。
- 暫定対応: **7 語を採用した。** 語数が多く分離の境界が細かい方を選べば、
  後から統合はできるが分割はできない。`.claude/rules/security.md` §1 は
  「絶対に守る境界」として `CLEANER` / `INSPECTOR` を区別しており、
  6 語では表現できない。`packages/db/src/schema/user.ts` の `ROLES` に固定し、
  `schema.spec.ts` が並びごと検証している。
  **契約書 §2.10 / §4 を 7 語へ書き換えるか、対応表を追記するかは人間の判断を待つ。**
  §4 の権限マトリクスは P0-10 がそのままテストケースにする予定なので、
  それまでに解決が要る。
- 補足（2026-08-12 / P0-10 実装中）: **P0-10 は §4 のビジネス表を実装対象から外した。**
  権限マトリクスの枠組みだけを作り、実装したのは security.md §1 が
  「絶対に守る境界」として明記した項目と、P0 に実体のある資源
  （組織・税務プロファイル・ユーザー・施設）の 11 アクションに限る。
  §4 の行（単価・契約条件、請求の確定、シフトと割当、清掃員の氏名…）は
  1 つも転記していない。
  写像が推測になるため。特に **§4 の `OWNER` は「自施設・清掃員氏名 ×・
  個人別実績 ×」で、security.md §1 の `OWNER`（組織全体）とは別概念**であり、
  ここを取り違えると「組織オーナーから清掃員の氏名を隠す／隠さない」という
  逆向きの実装になる。`PLATFORM_ADMIN` に至っては 7 語に対応が無い
  （プラットフォーム運営であって組織内ロールではない）。
  **各画面の権限は、その画面を作る task が `PERMISSION_ACTIONS` に 1 行足す**
  形にしてあるので、§4 の解決を待つのは各 task でよい。
  この問いは P0-10 の完了によって解消していない。

### #012 `docs/tasks/P0-06.md` が参照する `PK-SPEC-P0.md §5.2` が存在しない
- 提起: 2026-08-11 / P0-06 実装中
- 内容: P0-06 の task は 仕様 に `docs/PK-SPEC-P0.md §5.2` を挙げているが、
  現物の PK-SPEC-P0.md は v1.2 の差分文書で、§0 / §5.3 / §9 / §13 / §19〜§27 しか
  含まない。§5.2（データモデル）は v1.0 の節番号と思われるが、v1.0 本体が
  リポジトリに無い。**13 テーブルの列定義は仕様のどこにも存在しない。**
  同様に P0-11 の §5.4、P0-12 の §12、P0-16 の §7.2 / §8 も現物に無い。
- 影響: P0-06、P0-11、P0-12、P0-16。
- 暫定対応: `.claude/rules/*`・`docs/PK-IMPL-CONTRACT.md`・PK-SPEC-P0 §23 / §24・
  後続 task（P0-07 / P0-12 / P0-16 / P0-17 / P0-22）の記述から逆算して確定した。
  由来は各スキーマファイルの doc コメントに書いてある。
  **v1.0 本体を取り込むか、task の参照先を書き換えるかは人間の判断を待つ。**

### #013 清掃スタッフのログイン識別子が文書間で食い違う
- 提起: 2026-08-11 / P0-06 実装中
- 内容: `.claude/rules/security.md` §2 と `docs/PK-SPEC-P7.md` §2.4 の
  ログイン案内カードは「施設コード＋スタッフ番号＋PIN 4 桁」。
  一方 `docs/PK-IMPL-CONTRACT.md` §3.1 の画面 01 は「電話番号＋PIN」。
  電話番号を採ると従業員の連絡先を保存することになり、保持する個人情報が増える。
- 影響: P0-06（`user` の列）、P0-09（PIN ログイン）、P1-07（M-01 ログイン画面）。
- 暫定対応: **security.md に従い `staffNumber` を採用し、電話番号の列を作らなかった。**
  保存する個人情報は少ない方へ倒す（security.md §3 の方針）。
  後から列を足すのは非破壊だが、消すのは 3 段階のマイグレーションが要る。
  **P0-09 の着手前に確定させること。**
- 補足（2026-08-11 / P0-08 実装中）: **管理系も同じ 3 フィールド構成に揃えた。**
  識別子は `orgShortId` + スタッフ番号 + 認証情報（DECISIONS #018）。
  当初 security.md にあった「施設コード」は使わない。施設コードでは組織を一意に
  決められず、`org_directory` からシャードを解決できないため。
  実装契約書 §7 / §8 の「電話番号＋PIN」との食い違いは未解決のまま残っている。
  電話番号方式を採る場合、認証のために従業員の連絡先を必須保持することになり、
  security.md §5（従業員データの最小化）と逆行する点も判断材料にすること。

### #015 課金モデルが 2 通り併存している
- 提起: 2026-08-11 / P0-06 実装中
- 内容: `docs/PK-SPEC-P7.md` §3.1 はモジュール別の価格
  （Platform ¥4,980 / 組織・月、Housekeeping Core ¥3,980＋¥80/室、
  稼働照合 Audit ¥4,980 / 施設・月 …）で書かれている。
  一方 `docs/PK-BIZ-PLAN.md` §4〜§5 は版数（Base 240 円/室 ・ Pro 420 円/室 ・ Ent）で
  書かれており、「3 段階の機能別プランを採用しない理由」まで記載がある。
  どちらが正か、両立するのか（版数で束ね、内訳がモジュール）が不明。
- 影響: P0-12（エンタイトルメント）、P7-03（トライアル）、P7-04（Stripe 連携）。
- 暫定対応: 契約の単位（`subscription`: 版数・状態・期間）と機能の可否
  （`moduleEntitlement`: モジュール別の on/off）を**別テーブルに分け**、
  どちらのモデルでも表現できる形にした。金額の計算式は持たせていない。
  `moduleCode` は P0-12 の `assertEntitlement(ctx, "AUDIT")` と揃えるため
  PK-SPEC-P7 §3.1 の名前を採った。**価格モデルの確定は P7-04 までに。**

### #018 ログイン API に `Idempotency-Key` を実装していない
- 提起: 2026-08-11 / P0-08 実装中
- 内容: CLAUDE.md §5 は「状態変更 API は `Idempotency-Key` ヘッダに対応する」と定める。
  ログインはセッションを作るので状態変更にあたるが、P0-08 では実装していない。
  共通の冪等基盤（キーの保管・再生応答）を持つ task は P1-05 で、現時点に土台が無い。
- 影響: P0-08（`/auth/login`）、P1-05（冪等基盤）。
- 暫定対応: 実装していない。ログインの再送は**新しいセッションが 1 本増えるだけ**で、
  データは壊れず、いずれも 12 時間で失効する。P1-05 が共通基盤を作るときに
  認証系も対象に含めるかを判断すること。

### #019 `docs/tasks/P0-10.md` が参照する `PK-SPEC-P0.md §4.2` が存在しない
- 提起: 2026-08-12 / P0-10 実装中
- 内容: P0-10 の task は 仕様 に `docs/PK-SPEC-P0.md §4.2` を挙げているが、
  現物の PK-SPEC-P0.md は §0 / §5.3 / §9 / §13 / §19〜§27 しか含まず、§4 が無い。
  #012 と同じ形の参照ずれ（v1.0 本体がリポジトリに無いことに起因すると思われる）。
  なお `docs/PK-IMPL-CONTRACT.md` にも §4.2 はあるが、そちらは「再清掃依頼の経路」で
  認可の話ではない。
- 影響: P0-10（実装済み）、および P0-10 を参照する後続 task。
- 暫定対応: 根拠を `.claude/rules/security.md` §1 と
  `docs/PK-IMPL-CONTRACT.md` §4.1・INV-31・INV-32、`docs/PK-SPEC-P0.md` §19.4 に置いた。
  この 4 つで権限判定の要件（サーバー側検証・404・クライアント値を信用しない・
  ロール別の境界）は揃う。**v1.0 本体が見つかった場合は §4.2 を読み直し、
  食い違いがあれば起票すること。**

---

### #032 タスクの語彙が実装契約書と仕様書で食い違う
- 提起: 2026-08-12 / P1-01 実装中
- 内容: `docs/PK-IMPL-CONTRACT.md` §2.1 は `taskType` を
  `CHECKOUT_CLEAN` / `STAY_CLEAN` / `INSPECTION` / `REWORK`、`status` を
  `TODO` / `IN_PROGRESS` / `PAUSED` / `ON_HOLD` / `COMPLETED` / `REWORK_REQUIRED`
  と定める。一方 `docs/PK-SPEC-P1.md` §2.1 は `CHECKOUT` / `STAYOVER` /
  `DEEP` / `COMMON_AREA` / `RECHECK` と、9 状態（`CREATED` / `ASSIGNED` /
  `AWAITING_INSPECTION` / `CANCELLED` を含む）を定める。
  同 §2.4 と §2.1 のチェックリストも食い違う（契約は 3 値 `value`、
  仕様は `isChecked: Boolean`）。CLAUDE.md §7 は実装契約書を優先すると
  定めるが、契約書の 6 状態では §5.1 の状態機械を表現できない。
  **#011（`role` の語彙）と同じ性質の食い違いで、そちらは未回答のまま。**
- 影響: P1-01（実装済み）、P1-02〜P1-23、P2 の検査フロー、P4 の照合、P5 の請求。
  **語彙は永続データなので後から変えられない。**
- 暫定対応: 項目ごとに**表現力の広い側**を採った（`docs/DECISIONS.md` #031）。
  `taskType` / `status` は仕様書、チェックリストの実施結果は
  実装契約書の 3 値（INV-22 が不変条件として明示しているため）。
  人間が裁定したら、**負けたほうの文書を直すこと。** どちらの語彙も
  ID や enum として保存済みになる前に決めるほど傷が浅い。

### #033 Cron（`scheduled`）が使うテナント文脈のロールが仕様に無い
- 提起: 2026-08-12 / P1-03 実装中
- 内容: `TenantContext` は `role` を必須で持ち、`scopeToProperties()` が
  組織全体か施設スコープかを決めるのに使う（architecture.md §2 第 1 層）。
  日次バッチ（PK-SPEC-P1 §3.2）はセッションを持たないため、
  **バッチが名乗るロールが決まっていない。**
  加えて、組織の一覧を得る手段が SHARD_00 の `org_directory` しかない。
- 影響: P1-03（実装済み）、および今後の Queue コンシューマ全般
  （P2-10 / P3-09 / P4-14 / P5-06 / P6-04 / P7-09）。**同じ問いに全部が当たる。**
- 暫定対応: `listOrganizationDirectory()`（SHARD_00 の 1 表を読むだけ。
  全シャード走査ではない）を足し、バッチは組織ごとに
  `role: "ORG_ADMIN"` の文脈を組み立てる。**`ROLES` に `SYSTEM` を
  足していない**（7 ロールは security.md §1 の語彙で、増やすと権限
  マトリクスの全セルが動く）。バッチは `assertPermission()` を一度も
  呼ばないのでロールが認可に効くことはないが、**監査ログを書く
  コンシューマが出てきたら「誰が操作したか」が説明できなくなる。**
  そのときまでに、システム操作者の表し方を決めること。

### #034 `room.housekeepingStatus` を持つ表が無い（**解決済 / P1-16**）
- 解決: 2026-08-12 / P1-16 が `room.housekeepingStatus` を追加し
  （migration 0004）、`packages/engine/src/roomStatus.ts` の
  `housekeepingStatusFor()` で §11.1 の表を実装した。以下は経緯。
- 提起: 2026-08-12 / P1-01 実装中
- 内容: `docs/PK-SPEC-P1.md` §11.1 はタスクの状態変化に応じて
  `Room.housekeepingStatus` を `DIRTY` / `IN_PROGRESS` / `READY` /
  `INSPECTING` / `BLOCKED` へ動かすと定めるが、**その列は
  `docs/tasks/P1-01.md` の やること（8 表）に無く、`room` にも無い。**
  客室ステータス同期は `docs/tasks/P1-16.md` の担当。
- 影響: P1-16、P1-15（W-03 / M-10 の客室ボード）、
  および OPEN_QUESTIONS #029（rollup の列と客室状態の対応）。
- 暫定対応: **列を足していない**（task に書かれていないことを実装しない /
  CLAUDE.md §1-4）。P1-05 の状態遷移は `cleaningTask.status` だけを動かす。
  P1-16 が `room` に列を追加し、`applyTransition()` の呼び出し側で
  同期させること。**列の追加は後方互換なので順序の問題は無い**
  （architecture.md §6）。同期を実装するとき、#029 の
  「rollup の列と客室状態の対応」も一緒に決められる。

### #035 M-02 の並び順が仕様書とプロトタイプで食い違う
- 提起: 2026-08-12 / P1-08 実装中
- 内容: `docs/PK-SPEC-P1.md` §9.2 の要件は
  **「作業中 → 差戻し → 未着手（priority 昇順）→ 入室不可 → 完了」**。
  一方 `ui-prototypes/mobile/pk-02-today-tasks.html` の「並び順の規則」表は
  **「再清掃 → 作業中 → 未着手 → 入室不可 → 完了」**で、先頭 2 つが逆。
  プロトタイプ側は「再清掃は READY 遅延に直結する」を理由に挙げている。
- 影響: P1-08（M-02）、P2 の検査・差戻し（`REWORK` を実際に作る側）。
- 暫定対応: **仕様書の順を採った**（`packages/engine/src/taskBoard.ts` の
  `GROUP_ORDER`）。CLAUDE.md §7 は「ui-prototypes は**見た目**の正」と定めており、
  並び順は見た目ではなく動きなので仕様書を優先した。
  **P1 では `REWORK` が発生しない**（差戻しは P2）ため、どちらでも
  現時点の画面は変わらない。P2 が差戻しを作るときに決め直すこと。

### #036 写真の `capturedAt` が仕様書と実装契約書で食い違う
- 提起: 2026-08-12 / P1-11 実装中
- 内容: `docs/PK-SPEC-P1.md` §7.2 は「EXIF から撮影時刻を抽出、GPS 情報を除去」と
  書き、`packages/db` の `taskPhoto.capturedAt` も「EXIF の撮影時刻」としている。
  一方 `docs/PK-IMPL-CONTRACT.md` §2.5 は `photos[].capturedAt` を
  **「サーバー時刻で上書き」**と定める。
- 影響: P1-11、P4（写真の時刻を稼働照合のシグナルに使う場合）。
- 暫定対応: **契約書に従いサーバー時刻を入れた**（CLAUDE.md §7「実装契約書が
  仕様書に優先する」）。加えて、クライアントは canvas で再エンコードするため
  **EXIF は端末側で丸ごと消えており、撮影時刻を抽出する対象が残らない**
  （GPS だけを選んで消す実装より消し漏れが起きない / INV-11）。
  オフラインで溜めた写真は、送信した時刻が `capturedAt` になる。
  **撮影時刻そのものが要るなら、端末時刻を別の列で受ける設計が要る**
  （その場合も位置情報は受けない）。P4 が必要とするか判断すること。

### #037 PIN の初回変更を強制する画面が P1 の task に無い
- 提起: 2026-08-12 / P1-07 実装中
- 内容: `.claude/rules/security.md` §2 は PIN の「初回変更を強制」と定め、
  `pinLogin()` は `pinMustChange` を返す（P0-09）。しかし
  **PIN を変更する画面の task が `docs/tasks/` に無い。**
  `docs/PK-SPEC-P1.md` §9.1 のモバイル画面一覧にも現れない
  （M-11「自分の実績・設定」が最も近いが、P1-17 のやることは実績のみ）。
- 影響: P1-07、P1-17（M-11）、運用（初期 PIN のまま使い続けられる）。
- 暫定対応: **`pinMustChange` を見ずにログインを通した**（画面が無いので
  強制しても入れなくなるだけ）。M-11 を作る task が PIN 変更を持つか、
  独立した画面を起こすかを決めること。**API 側（PIN の更新）も未実装。**


### #058 除外ルール①「値 0 かつ bedsUsed > 0」を全品目に当ててよいか
- 提起: 2026-08-13 / P3-08 実装中
- 内容: `docs/PK-SPEC-P3.md` §5.3 の除外ルール①は
  **「値が 0 かつ `bedsUsed > 0`（明らかな入力漏れ）」**。バスタオルなら
  「人が泊まったのにタオル 0 枚」は確かに入力漏れだが、**同じ判定を
  アメニティにも当てると、使われなかった日の 0 がすべて消える。**
  ミネラルウォーター・追加布団・カミソリは「泊まったが使わなかった」が
  正常な観察で、それを落とすと母数が「使われた日」だけになり、
  中央値も p90 も実態より高く出る。
- 影響: P3-08（実装済み）、P3-09（週次バッチ）、**P4 の照合精度**。
  ベースラインが高く出るぶん、P4 は差異を**見逃す**側に倒れる
  （過検知ではない）。除外率も上がるため、W-22 の 15% 警告が
  常時点灯する施設が出うる。
- 暫定対応: **仕様どおり全品目に当てた**（`outlierReasonOf()` の①）。
  推測でリネンだけに絞ると、仕様と実装が黙って食い違う。
  除外理由は `ZERO_WITH_BEDS_USED` として 1 件ずつ記録してあるので、
  P3 のデータ蓄積期間（P3-13、4 週間）に**実データで除外率の内訳を
  確認できる。** 品目別の除外率がそこで見える。
- 決める人: P3-13 の蓄積結果を見てから。①をリネン・タオル類
  （`LINEN_ITEM_CODES`）だけに絞るか、`observationConfig` で品目ごとに
  「0 が正常な品目」を持たせるかを決める。**P4 着手前に決めること。**

### #063 抑制条件が参照する施設の 2 つの属性に対応する列が無い
- 提起: 2026-08-13 / P4-01 実装中
- 内容: `docs/PK-SPEC-P4.md` §4.1 の抑制条件に
  **「施設の `occupancyLinked = false`（A 系統を要するルール）」**、
  §4.2 の確信度調整に **「施設の運用開始から 60 日以内」**（−10）と
  **「開業・導入から 30 日以内」**（§4.1、baseline 未成熟なルール）が
  あるが、`property` に**どちらの列も無い。**
  `occupancyLinked` は「その施設が稼働記録の連携を持っているか」、
  運用開始日は「いつからこの施設で ProofKeeping を使い始めたか」を表す。
- 影響: §4.1 の 6 条件のうち 2 つ、§4.2 の 4 条件のうち 1 つが効かない。
  抑制が弱いぶん**誤検知が増える側**に倒れる（見逃しではない）。
  §0.3 の出荷判定「誤検知率 30% 未満」に直接効く。
- 暫定対応: `packages/engine` は純粋関数なので、両方を `RuleContext` の
  入力（`property.occupancyLinked` / `property.daysSinceOperationStart`）
  として受け取る形にした。**engine 側は既に効く。** 呼ぶ側が値を渡せない
  だけで、渡せるようになれば実装は変わらない。
- **2026-08-14 追記（P4-05）— 列を足さずに導く形で配線した**（DECISIONS
  #110）。`occupancyLinked` は**直近 30 日に稼働記録が 1 件でもあるか**、
  運用開始日は `property.createdAt` を代用する。列を足して既定 `false` に
  すると、切り替える画面（W-13 は P6）が出来るまで A 系統を要するルールが
  1 つも動かない。**この追記で作業は止めていない。**
- 決める人: **まだ開いている。** P6 で `integration` の状態が入ったら、
  `occupancyLinked` をそちらから導くか、明示的な列
  （`property.occupancy_linked` / `property.operation_started_at`）に
  切り替えるかを決めること。engine は値を受け取るだけなので、
  どちらでも**呼び出し側の 1 行**で済む。


---

## 解決済

### #001 フロントエンドフレームワーク
- 提起: 未着手 / P0-01
- 回答: 2026-08-12 / P0-14 で実装
- 内容: Remix on Workers とするか、Next.js を OpenNext で載せるか。
  あわせて **`.tsx` が ESLint で検査できない**問題（`apps/web/tsconfig.json` の
  `include` が `src/**/*.ts` のみで、`jsx` がどの tsconfig にも無い）を
  P0-14 が同時に解く約束になっていた（P0-04 からの申し送り）。
- 決定: **React Router v8 の framework mode**（`react-router` 8.3.x +
  `@react-router/dev` + `@cloudflare/vite-plugin`）。技術検証は行わない
  （人間の判断）。Remix v2（`@remix-run/*`）は 2025 年以降メンテナンスのみで、
  **Remix の直系の後継が React Router の framework mode** である。
  ファイル規約・`loader` / `action` の書き方は Remix と同じ。
  `tsconfig` は `jsx: "react-jsx"` / `jsxImportSource: "react"`。
  詳細は `docs/DECISIONS.md` #026。
- 影響: P0-14 以降の全 UI タスク。
  - `apps/web` は **API（Hono）と画面（React Router）が 1 つの Worker に同居**する。
    `src/index.ts` の catch-all が `/api/**` 以外を React Router へ渡す。
  - `.tsx` が `tsc` と ESLint の対象になった。`pk/no-literal-string` と
    `pk/no-forbidden-words` が実ファイルに当たることを確認済み。
    設定が外れると `tests/toolchain/workspace.spec.ts` が落ちる。
  - `pnpm dev` / `pnpm --filter @pk/web build` の実体は vite。
  - ルートは `src/routes.ts` に**明示列挙**する。ファイル規約にすると
    `src/routes/api/v1/*.ts`（Hono のハンドラ）が UI ルートとして拾われる。
- 残る注意: **UI スタックの Tailwind CSS + shadcn/ui は未導入**（CLAUDE.md §2）。
  P0-14 は v3 レイアウト標準の寸法とトークンを確かめる最小の CSS だけを置いた。
  導入は画面を作る task（DECISIONS #028）。

### #016 施設列を持たない表を施設スコープロールがどこまで見てよいか
- 提起: 2026-08-11 / P0-07 実装中
- 回答: 2026-08-12 / P0-10 で実装
- 内容: `user` / `membership` / `organization` / `organizationTaxProfile` は
  `propertyId` を持たないため、`scopeToProperties()` が掛からない。
  `.claude/rules/security.md` §1 は `CLEANER` / `INSPECTOR` について
  差異レポート・忘れ物・請求の境界を定めているが、**組織のユーザー一覧を
  見てよいかには触れていない。** 清掃スタッフが同僚の一覧（表示名・スタッフ番号・
  ロック状態）を取得できてよいかは推測になる。
- 決定: **読み取りは組織全体、書き込みは自施設のみ。** 施設スコープロール
  （`PROPERTY_MANAGER` / `INSPECTOR` / `CLEANER`）は施設列を持たない表を
  組織全体で参照でき、書き込みは自分の施設に関連するものに限る。
  `user.read` は 7 ロールすべてに `ORG`、`user.write` は `PROPERTY_MANAGER` に
  `ASSIGNED`。詳細と却下した選択肢は `docs/DECISIONS.md` #023。
- 影響: P0-07（`listUsers`）、P0-10（権限マトリクス）、P1-14（人員配分画面）。
  - **リポジトリ層は変更していない。** `NO_PROPERTY_SCOPE` はそのまま。
    読み取りを組織全体に開くという回答が、既存の実装をそのまま追認した。
  - 「書き込みアクションで `ORG` を持てるのは組織全体ロールだけ」を
    `apps/web/src/lib/auth/permission.spec.ts` の不変条件として固定した。
  - `ASSIGNED` は**部分集合**で判定する（交差ではない）。帰結として
    `PROPERTY_MANAGER` は施設割当を持たないユーザーを作れない。
    招待 API は「招待と施設割当を同時に行う」形にすること。
- 残る注意: P1-14 が担当施設のスタッフだけを出す必要に迫られたら、
  **リポジトリ層ではなくその画面の絞り込みとして実装する**こと。
  `user` を引くたびに `property_assignment` と結合する形にすると後から外せない。

### #017 PIN のハッシュ方式だけ bcrypt が残っている
- 提起: 2026-08-11 / P0-08 実装中
- 回答: 2026-08-12 / P0-09 で実装
- 内容: パスワードのハッシュを PBKDF2-SHA256 210,000 回へ変更した（DECISIONS #019）が、
  security.md §2 の PIN 行は「bcrypt cost 10」のまま据え置いた。P0-09 は
  **PIN のためだけに bcryptjs（純 JS）を導入する**か、PIN も PBKDF2 に揃えるかを
  選ぶことになっていた。
- 決定: **PIN も PBKDF2-SHA256 に揃え、反復回数だけ 50,000 へ下げる。
  bcryptjs は導入しない。** 4 桁 PIN は候補が 10,000 通りしかなく、
  50,000 回（実測 9.6ms）でも 210,000 回（実測 37.8ms）でも総当たりは現実的な時間で
  終わる。**KDF の強度差が防御の成否を分けない**一方、反復回数は現場系ログインの
  応答時間に直に乗る。50,000 は「cost 10 = cost 12 の 1/4 の作業量」を
  210,000 に当てた値（210,000 ÷ 4 = 52,500 を丸めた）。詳細は DECISIONS #021。
- 影響: security.md §2 の PIN 行を改訂。PBKDF2 の機構を `password.ts` から
  `apps/web/src/lib/auth/pbkdf2.ts` へ抽出し、パスワードと PIN が共有する。
  `packages/db` の依存は増えていない。

### #006 シャード明示マッピングを置く KV binding 名
- 提起: 2026-08-11 / P0-02 実装中
- 回答: 2026-08-11
- 内容: `.claude/rules/architecture.md` §1 の `resolveShard()` のコード例は
  `env.KV.get("shard:{organizationId}")` と binding 名 `KV` を使っている。
  しかし P0-02 の task が作る KV namespace は `SESSION` / `RATELIMIT` /
  `CONFIG` / `CREDENTIALS` の 4 本で、`KV` という namespace は存在しない。
- 影響: P0-03（`resolveShard()` の実装）
- 決定: **専用の namespace `SHARD_MAP` を追加する。** `CONFIG` に相乗りさせない。
  `shard:{organizationId}` が失われても `resolveShard()` はエラーにならず
  `fnv1a32` のフォールバックに落ちるため、同一テナントのデータが複数シャードへ
  無警告で分裂する。`CONFIG` は一括更新・一括削除・TTL 失効の対象であり同居できない。
  `SHARD_MAP` には TTL を設定しない。詳細は `docs/DECISIONS.md` #006。

### #009 `orgShortId` のグローバル一意性をどこで保証するか
- 提起: 2026-08-11 / P0-05 実装中
- 回答: 2026-08-11 / P0-06 で実装
- 決定: **SHARD_00 の `org_directory` テーブルと主キー制約で担保する。**
  `packages/db/src/router.ts` の `getGlobalDb(env)` が SHARD_00 の Drizzle
  インスタンスを返し、`packages/db/src/orgDirectory.ts` が
  `createOrgShortIdTaken()` / `reserveOrgShortId()` を提供する。
  KV namespace は増やさない（`wrangler.toml` は変更していない）。
  詳細と却下した選択肢は `docs/DECISIONS.md` #014。
- 残る注意: テーブル定義は 16 シャードすべてに流し、**実体は SHARD_00 のみ**を使う。
  SHARD_00 にだけ作ると `schema_version` が食い違い、起動時の不一致検出が
  正常時に発火する。組織作成は「採番 → 予約 → 組織本体」の順を守ること。

### #010 P0-06 が作る 13 テーブルの `entityPrefix` が仕様に無い
- 提起: 2026-08-11 / P0-05 実装中
- 回答: 2026-08-11 / P0-06 で実装
- 決定: 閉じたレジストリのまま 13 個を追記した。
  `org` / `tax` / `seq` / `usr` / `mem` / `asgn` / `bldg` / `flr` / `rtyp` /
  `room` / `sub` / `ent` / `audit`。`mem` / `room` は `docs/PK-SPEC-P2.md` の
  記述に合わせ、残りは P0-06 の決定。由来は `docs/DECISIONS.md` #013。
- 残る注意: **ID は永続データなので接頭辞を後から変更できない。**
  `packages/db/src/id.spec.ts` が並びと綴りを固定している。

### #014 メールアドレスから組織を解決する手段が無い
- 提起: 2026-08-11 / P0-06 実装中
- 回答: 2026-08-11 / P0-08 で実装
- 内容: `user` は組織スコープの表なので email の一意性は組織内でしか成立せず、
  「メール＋パスワード」でログインする管理系（security.md §2）は、入力された
  メールから組織 → シャードを解決する手段を持たなかった（全シャード走査は禁止）。
- 決定: **ログイン識別子からメールを外し、`orgShortId` + スタッフ番号 + 認証情報の
  3 フィールドにした。** 組織の解決は SHARD_00 の `org_directory` を引く既存の
  `lookupOrganizationId()` で足りる。`email_directory` は作らない。
  メールアドレスは通知の送信先としてのみ保持する。詳細は `docs/DECISIONS.md` #018。
- 残る注意: `user.staff_number` が全ロールの必須項目になった。列は後方互換のため
  null 許容のままだが、認証経路は null を弾く。ユーザー作成側（P0-18 seed・
  将来の招待画面）は**スタッフ番号を必ず採番すること。**

### #029 `dailyPropertyRollup` の列と §23.3 の応答が対応しない
- 提起: 2026-08-12 / P0-21 実装中
- 内容: `docs/PK-SPEC-P0.md` §23.3 の応答例は `ready` / `inProgress` / `dirty`
  （**客室の状態**の数）だが、§19.6 の `dailyPropertyRollup` が持つのは
  `totalTasks` / `completedTasks` / `reworkTasks`（**タスク**の数）。
  1 つの客室に複数のタスクが立つ設計なので、両者は 1 対 1 にならない。
  仕様の中に対応の定義が無い。
- 影響: P0-21（実装済み）、P1（客室ステータスを持つ task）、P5-xx
  （月次レポートも同じ rollup を読む — PK-SPEC-P5 §517）。
- 暫定対応: **rollup の列名のまま返した**（DECISIONS #030）。
  `PropertySummary` に `hasRollup` を置き、集計が無い施設には数字を出さない。
  P0 の間は `cleaningTask` が無いので rollup は常に空で、表示は変わらない。
  客室ステータスを持つ task が、①対応を定義するか ②rollup に列を足すか
  を決めること。**列を足す場合は再計算コンシューマの冪等性の検証範囲が
  広がる**（§19.6 MUST）。

### #030 Sentry を採用するかが未決のままヘルスチェックの task に含まれている
- 提起: 2026-08-12 / P0-20 実装中
- 内容: `docs/tasks/P0-20.md` の完了条件は「Sentry にエラーが集約される」だが、
  `docs/PK-SPEC-P0.md` §20（未決事項）は
  「Sentry を使うか、Cloudflare の Workers Observability に寄せるか」を
  **未決として挙げている。** 採用先が決まっていないので、どちらの SDK を
  入れるかも、DSN をどこから渡すかも決められない。
- 影響: P0-20、および全 task のエラー報告経路。
- 暫定対応: **`/api/health` だけを実装した。** `EnvSecrets` に `SENTRY_DSN` の
  宣言はあるが（P0-02）、読む処理は無い。採用先が決まってから、
  エラー報告の入口（`apps/web/src/middleware/index.ts` の `apiErrorHandler`）
  1 か所に足すこと。**各所に散らさない。**

### #031 `pnpm db:seed` を実行できる経路が無い
- 提起: 2026-08-12 / P0-18 実装中
- 内容: シードの実体（`packages/db/src/seed.ts`）と、`hashPin` / `hashPassword`
  を束ねる入口（`apps/web/src/lib/seed/runSeed.ts`）は実装した。しかし
  **node から直接実行できない。** `apps/web` の相対 import は `.js` 指定子で
  書かれており、node の型剥がしでは `.ts` へ読み替えられない
  （`scripts/db-migrate.ts` が `packages/db/src/migrate.ts` を呼べているのは、
  あちらが import を 1 つも持たないため）。実行するには Workers の binding
  （D1 / KV）も要り、実在するのは shard-00 だけ。
- 影響: P0-18、P0-02、および実機確認全般。
- 暫定対応: `db:seed` を配線していない（`tests/toolchain/workspace.spec.ts` の
  `DEFERRED_ROOT_SCRIPTS` に残してある）。P0-02 が bindings を作った後、
  `wrangler dev` から `runSeed()` を呼べる入口を足すこと。
  **`packages/db/src/seed.ts` を import の無い形に書き換える案は採らない。**
  drizzle のスキーマ（実行時の値）が要るので、書き換えると SQL を手で
  組むことになり、スキーマとの対応が切れる。

### #038 `unblock` のあとの客室ステータスが §11.1 の表に無い
- 提起: 2026-08-12 / P1-16 実装中
- 内容: `docs/PK-SPEC-P1.md` §11.1 の対応表は `block` → `BLOCKED` を定めるが、
  **`unblock` の行が無い。** 「変更しない」と読むと、入室できるように
  なった客室の盤面（W-03 / M-10）が「入室不可」を出し続ける。
- 影響: P1-16、P1-15（客室ボードの見え方）、P4（R010 の母数）。
- 暫定対応: **`unblock` → `DIRTY`** にした（`packages/engine/src/roomStatus.ts`）。
  表の 1 行目「タスク生成時 = DIRTY」と同じ意味（まだ清掃していない）へ戻す
  向き。`READY` へ戻す案は採らない（清掃せずに引き渡せる状態になる）。
  **仕様側で行を足すか、この扱いを追認するかを決めてほしい。**

### #039 自動配分の「担当フロアの希望順」を表すデータが無い
- 提起: 2026-08-12 / P1-14 実装中
- 内容: `docs/PK-SPEC-P1.md` §4.1 のアルゴリズム 2 は「出勤スタッフを
  担当フロアの希望順に並べる」と定めるが、**希望フロアを保持する列も表も
  存在しない。** `property_assignment` は施設までしか持たない。
  同じく「出勤スタッフ」を表すシフトのデータも無い（Workforce は P8）。
- 影響: P1-14、P8-xx（Workforce）。
- 暫定対応: **スタッフ番号の昇順**で並べた（説明可能な順序）。施設に
  割り当てられた有効なメンバー全員を候補にし、出勤の判定は行っていない。
  希望フロアを入れるなら `property_assignment` に列を足すか、
  P8 のシフトを待って `planAutoAssignment()` の第 2 引数を差し替えること
  （engine 側は並び順を引数で受け取る形なので、規則の変更で済む）。

### #040 M-11 の平均を 20 タスク未満で出すかが規則と仕様で食い違う
- 提起: 2026-08-12 / P1-17 実装中
- 内容: `.claude/rules/security.md` §5 は「個人単位の指標は対象期間
  20 タスク未満なら表示しない」と定める。一方 `docs/PK-SPEC-P1.md` §9.6 の
  例は本日 12 件で「平均 26分/室」を描き、`ui-prototypes/mobile/
  pk-14-personal-stats.html` も 11 件で「1部屋の平均 34分」を描く。
  **本人が自分の記録を見る画面（同 §5 MUST）に閾値を当てるのかが読めない。**
- 影響: P1-17、P2-15（指標算出）、P5（月次レポート）。
- 暫定対応: **平均だけを閾値で伏せ、件数と合計作業時間は常に出す**
  （docs/DECISIONS.md #035）。伏せているときは空欄にせず理由を出す。
  「本人の画面には閾値を当てない」で良ければ `MINIMUM_TASKS_FOR_AVERAGE` の
  適用箇所を管理側の集計（P2-15）へ移すこと。

### #041 施設変更の確認ダイアログに住所を出すか（プロトタイプ 04 の Q1〜Q5）
- 提起: 2026-08-12 / P1-23 実装中
- 内容: `ui-prototypes/mobile/pk-04-property-change.html` は確認ダイアログに
  **住所・担当フロア・この施設の残り件数・移動の警告文**を描き、末尾で
  「確認していただきたい点」として 5 問（住所を出すか / 移動の警告文は要るか /
  「今日はもう確認しない」を付けるか / 同じ施設に戻る場合も確認するか /
  移動時間の経過を見るか）を未回答のまま残している。
  `docs/PK-SPEC-P1.md` §19.8 の図は**施設名と部屋番号だけ**で、住所を含まない。
- 影響: P1-23、P8（シフトと移動時間が入ったときの Q5）。
- 暫定対応: **仕様の図に合わせた**（前施設名 → 施設名・部屋番号・清掃種別）。
  住所は出していない。Q4（同じ施設に戻る場合）はプロトタイプの表どおり
  確認を出す（`lastWorkedPropertyId()` が直前の施設を返すので自然にそうなる）。
  Q3（今日はもう確認しない）は付けていない。**住所を出すなら `property.address`
  を M-02 の一覧まで運ぶ必要がある**（いまは `my-day` に載せていない）ので、
  出す判断になった時点で応答の形が変わる。

### #042 施設選択画面が「翌日以降」に何日先まで出すか
- 提起: 2026-08-12 / P1-22 実装中
- 内容: `docs/PK-SPEC-P1.md` §19.4 は「翌日以降のタスクは選択できるが、
  開始はできない」とだけ書き、**何日先まで出すかを定めていない。**
  プロトタイプ 03 は Q3 で「3 日先まで見せると情報量が増えるため、
  翌日のみとする」と自ら決めている（確認済みの印つき）。
- 影響: P1-22、P8（シフトが入って予定が先まで見えるようになったとき）。
- 暫定対応: **翌日 1 日ぶんだけ**にした（プロトタイプ 03 Q3）。
  `routes/m/selectProperty.tsx` が当日と翌日の 2 回だけ `my-day` を組み立てる。
  範囲を広げるなら、日数ぶんクエリが増える形をやめて業務日の範囲で
  1 回引く形へ変えること（`listTasks` は `businessDateFrom/To` を持つ）。

### #043 必須検査対象のうち 2 つに、参照する表・列がまだ無い
- 提起: 2026-08-13 / P2-02 実装中
- 内容: `docs/PK-SPEC-P2.md` §2.2 は SAMPLE の必須検査対象として 5 つを挙げる。
  そのうち 2 つは、いま参照できるものが無い。
  - **「設備不具合または忘れ物報告があるタスク」** — `IssueReport` /
    `LostItem` の表は §3.5・§3.6 に定義があるが、作るのは P2-11 / P2-12。
  - **「施設が『重点客室』として指定した客室」** — §2.2 に文言はあるが、
    §3 のデータモデルに**対応する列が無い**（`room` にも
    `PropertyInspectionPolicy` にも）。どこに持たせるかが決まっていない。
- 影響: P2-02（いま）、P2-11 / P2-12（忘れ物・不具合）、
  「重点客室」を設定する画面を作る task。
- 暫定対応: **規則そのものは `packages/engine` に実装してテストしてある**
  （`decideInspection()` の `hasReport` / `isPriorityRoom`）。
  呼び出し側（`apps/web/src/lib/task/inspectionDecision.ts`）が `false` を
  渡している。表と列ができた task がその 2 行を差し替える。
  **規則を後から足すのではなく、材料を後から差すだけで済む形にしてある。**
- 決める必要があること: 「重点客室」を `room` の列にするのか、
  施設ごとの客室 ID 一覧（別表）にするのか。前者なら CSV 取込の列も増える。

### #044 `property.inspectionRequired` と `propertyInspectionPolicy.mode` の二重管理
- 提起: 2026-08-13 / P2-01 実装中
- 内容: P1 は施設の検査要否を `property.inspectionRequired`（真偽）で持つ。
  P2 は `propertyInspectionPolicy.mode`（ALL / SAMPLE / NONE）を足した。
  **同じことを 2 か所で表せる状態になっている。**
- 影響: P2-16（P1 暫定機能の移行・削除）。
- 暫定対応: `propertyInspectionPolicy` の行が**無い**施設では
  `property.inspectionRequired` から `ALL` / `NONE` を組み立てる
  （`policyFromLegacyFlag()`）。行がある施設ではそちらが勝つ。
  architecture.md §6 の「破壊的変更は 3 段階」に従い、旧列の削除は
  移行を通した次リリースで行う。**いまは①新列追加の段階。**
- **回答（2026-08-13 / P2-16）: 移行した。旧列は次リリースで落とす。**
  `0011_p2_16_inspection_policy_backfill.sql` が全施設に行を作る
  （`inspectionRequired = true` → `ALL` / `false` → `NONE`）。
  **既に行がある施設は触らない**（W-02 の設定が正）。
  `createProperty()` と `seed()` も施設と検査方式を一組で書くようにした。
  読み取り側の `policyFromLegacyFlag()` は残してある。移行が届いていない
  シャードで既定の `ALL` に落ちると、危惧どおり全タスクが検査待ちで
  滞留するため。DECISIONS #091。
- 残っていること: **旧列 `property.inspectionRequired` の DROP と
  `policyFromLegacyFlag()` の削除**（architecture.md §6 の③）。
  次リリースで行う。**同じリリースでやらないこと。**

### #045 チェックイン予定時刻を持つ列がどこにも無い（§5.3 の「緊急」が効かない）
- 提起: 2026-08-13 / P2-05 実装中
- 内容: `docs/PK-SPEC-P2.md` §5.3 は「当日チェックイン予定時刻まで 30 分未満の
  タスクは**緊急**として最上位表示する」「`DailyRoomPlan` にチェックイン時刻が
  ない場合、施設既定の `Property.checkInTime` を使う」と定める。
  **どちらの列も存在しない。** `dailyRoomPlan` が持つのは `hasCheckin`（真偽）
  だけで、`property` に `checkInTime` は無い。
- 影響: M-08（P2-05）の並びの第 1 段、W-03 の緊急表示、§11.2 のワイヤーが
  出している「チェックインまで 18 分」。
- 暫定対応: **規則は `packages/engine/src/inspectionQueue.ts` に実装し、
  テストしてある**（`checkInAtMs` を受け取る）。呼び出し側
  （`lib/inspection/waiting.ts`）が `null` を渡しているので、いまは
  第 1 段が常に空になる。**列ができたら 1 行差せば効く。**
  `hasCheckin` を「緊急」に読み替えていない。チェックアウト主体の施設では
  大半の部屋にチェックインが立ち、**全件が緊急になって印が死ぬ。**
- 決める人: 列をどちらに置くか（`dailyRoomPlan.checkInAt` か
  `property.checkInTime` か両方か）。CSV 取込（P1-04）の列も増える。

### #046 M-08 / M-09 に清掃担当者の名前を出すか
- 提起: 2026-08-13 / P2-05・P2-06 実装中
- 内容: `docs/PK-SPEC-P2.md` §11.2 のワイヤーは各行に「清掃: 田中 / 完了 14:02」を
  描き、§11.3 も「清掃: 田中 / 14:02完了」を出す。
  一方 `ui-prototypes/mobile/pk-13-inspection.html` は
  **「この画面の最大の設計判断は、検査結果を入力するまで清掃員の名前を
  表示しないこと。名前を先に見ると、その人への印象が検査結果に影響する」**と
  明記し、記録後にだけ見せる設計にしている。
- 影響: M-08（P2-05）、M-09（P2-06）、`inspectionWaitingItemSchema`。
- 暫定対応: **名前を出さない側を採った。** 一覧・検査画面のどちらも
  `assigneeId` を応答に載せていない（API のレスポンスにも無い）。
  根拠は INV-06 / INV-07（個人単位の評価に使える形を作らない）と、
  プロトタイプが明示した設計意図。**仕様書のワイヤーには従っていない。**
  CLAUDE.md §7 は「ui-prototypes は見た目の正」と定めるが、ここは見た目
  ではなく「誰に何をいつ見せるか」なので、判断として記録する。
- 決める人: 検査後に担当者を確認できる導線が要るか（P2-09 W-07 証跡詳細で
  足りるか、M-09 の完了後に出すか）。

### #047 M-09 の画面設計が仕様書とプロトタイプで食い違う
- 提起: 2026-08-13 / P2-06 実装中
- 内容: `docs/PK-SPEC-P2.md` §11.3 は**項目別の 3 値入力**
  （合格 / 不合格 / 対象外、不合格には理由コード・コメント・写真）を定め、
  MUST として「画面初期値をすべて PASS にしない」「『全て合格』を置かない」を
  挙げる。一方 `ui-prototypes/mobile/pk-13-inspection.html` は
  **部屋単位の二択**（「問題なし」/「再清掃が必要」＋理由チップ）で、
  さらに解説パネルに「検査の対象: 全室ではなく抜き取り（既定 10%）」
  「未検査の扱い: **24 時間後に自動で合格**」と書いている。
- 影響: M-09（P2-06）、P2-04 の完了条件（項目ごとの FAIL に理由コード・
  コメント・写真）、§2.3（「検査なし」を「検査合格」として集計しない）。
- 暫定対応: **仕様書 §11.3 を採った**（項目別の 3 値）。理由は 3 つ。
  ① §11.3 の 2 つの MUST と P2 固有の絶対ルールが項目別入力を前提にしている。
  ② P2-04 の完了条件「1 項目でも FAIL があれば全体が FAIL」は、
     項目という単位が無いと成立しない。
  ③ プロトタイプの「24 時間後に自動で合格」は §2.3 が明確に禁じている
     （検査していないものを合格として記録しない）。抽出率 10% も
     `propertyInspectionPolicy.sampleRate` の既定 100 と食い違う。
  **プロトタイプ pk-13 は P2 仕様の確定前に描かれたものと判断した。**
- 決める人: pk-13 を §11.3 に合わせて描き直すか、`_archive/` へ移すか。
  現状のままだと、次に M-09 を触る task が同じ食い違いに当たる。

### #048 `AUDITOR` は証跡 ZIP を出力できるべきか
- 提起: 2026-08-13 / P2-10 実装中
- 内容: 証跡 ZIP の出力に `evidence.export`（`write: true`）を足し、
  security.md §1 の「`AUDITOR` は書き込み操作を一切できない」に沿って
  `AUDITOR` を `DENY` にした。**ZIP の生成は R2 へオブジェクトを書く操作**
  なので形式上は書き込みだが、**内容は読み取りの成果物**で、監査閲覧という
  役割からすると出力できないのは不便かもしれない。
- 影響: `AUDITOR` は W-07 の内容を画面で読めるが、書庫として持ち出せない。
  §16.2 の受け入れ条件（「ZIP 内の verify.txt で全ファイルを検証できる」）は
  誰が出力するかを定めていない。
- 暫定対応: **`DENY`。** 権限マトリクスの既定は「書き忘れが『見えすぎる』
  方向に壊れない」向きに倒す方針（`permission.ts` 冒頭）で、広げるのは
  根拠を持つ task の仕事。
- 決める人: 監査対応の運用で `AUDITOR` が証跡を持ち出す場面があるか。
  あるなら `evidence.export` を読み取り扱い（`write: false`）にして
  `AUDITOR: "ORG"` にするか、`evidence.exportRead` を別に切るかを決める。

### #049 W-07 に「清掃チェックリストと写真」の節が無い
- 提起: 2026-08-13 / P2-09 実装中
- 内容: §12.2 の表示順は 9 項目で、3 番目に「清掃チェックリストと写真」を
  挙げる。P2-09 はこれを実装していない。項目ごとの結果と写真は
  M-03 / M-04 / M-09 が持ち、W-07 から辿る導線が無い。
- 影響: §12.2 の 9 項目のうち 1 つが未達。ただし §16.2 の受け入れ条件
  （証跡の連鎖と写真ハッシュの検証）はすべて満たしている。
- 暫定対応: **足していない。** W-07 に写真を並べると、清掃者ごとの作業内容を
  一覧する面ができる（§1.3 / INV-07 が禁じる方向へ育つ余地）。写真は
  「整合性を確認」で**ハッシュとして**照合できる形にした。
- 決める人: W-07 に写真そのものを並べるか、ハッシュの照合結果だけで足りるか。
  並べるなら、担当者ごとに絞れない形にする必要がある。

### #050 `POST /api/v1/evidence/:id/corrections`（§6.4 の訂正）が無い
- 提起: 2026-08-13 / P2-09 実装中
- 内容: §14.2 は訂正の経路を挙げ、§6.4 は「訂正できるのは `ORG_ADMIN` のみ。
  理由必須」と定める。DB 側は P2-08 が `correctsSnapshotId` /
  `correctionReason` を用意済みで、足りないのは経路と判定だけ。
- 影響: 誤入力の訂正ができない。**元の証跡が壊れる方向の欠落ではない**
  （訂正は新レコードの追加なので、無くても記録は正しいまま残る）。
- 暫定対応: **P2-09 / P2-10 のどちらの完了条件にも無いので足していない。**
- 決める人: 訂正の入口をどの画面に置くか（W-07 に置くと、証跡の画面から
  証跡を書ける形になる）。P2-16（P1 暫定機能の移行・削除）か、
  §6.4 を担当する task が決める。

### #051 忘れ物・不具合の写真アップロード経路が無い
- 提起: 2026-08-13 / P2-11・P2-12 実装中
- 内容: §14.3 は `POST /lost-items/:id/photos` と `POST /issues/:id/photos` を
  挙げ、§7.5 は「忘れ物全体が分かる写真 1 枚を必須」、§8.1 は
  「写真 1 枚以上（安全上撮影困難な場合を除く）」と定める。
  P2-11 / P2-12 は**表（`lostItemPhoto` / `issuePhoto`）と
  `createLostItemPhoto()` / `createIssuePhoto()` までを用意し、
  HTTP の受け口を作っていない。**
- 影響: 現状は写真ゼロのまま登録できる。§7.5 / §8.1 の「必須」が
  効いていない（`PHOTO_REQUIRED` のエラーコードは用意してある）。
- 暫定対応: 受け口を作らなかった。
- **回答（2026-08-13 / P2-13）: 手順を抜き出して 4 経路で共有した。**
  2 つの既存経路を読み直したところ、**すでに `sanitizeImage()` /
  `photoStorageKey()` / `sha256Hex()` を共有しており**、違うのは
  「親の行を引く・権限・INSERT 先」の 3 つだけだった。分かれていたのは
  書き方ではなく、**同じ手順が 2 回書き写されていた**こと。
  `lib/photo/pipeline.ts` に「サイズ・形式の検査 → EXIF 除去 →
  SHA-256 → R2」を固定し、4 経路（清掃 / 検査 / 忘れ物 / 不具合）が
  そこを通る。経路ごとに違う 3 つは呼び出し側に残した
  （`assertPermission()` を引数で受けると、渡し間違いが権限の穴になる）。
- 状態: **解決。** `POST /api/v1/{lost-items,issues}/:id/photos` を追加。

### #052 施設ごとの忘れ物保持日数を持つ列が無い
- 提起: 2026-08-13 / P2-11 実装中
- 内容: §7.3 の表は衣類・バッグ・その他を「施設設定（既定 90 日）」、
  食品を「施設設定（既定 当日）」と定めるが、**`property` に
  その設定を持つ列が無い。**
- 影響: 現状は engine の既定（90 日 / 当日）が常に効く。施設ごとに
  変えられない。`retentionDaysFor()` は施設設定を引数で受け取る形に
  してあるので、列が増えれば渡すだけ。
- 暫定対応: `routes/api/v1/lostItems.ts` が `propertyRetentionDays: null`
  を渡している（＝常に既定）。
- 決める人: `property` に 1 列（`lostItemRetentionDays`）を足すか、
  施設設定をまとめた表（W-11 施設設定画面）を作るときに一緒に入れるか。

### #053 忘れ物・不具合の「写真必須」をサーバー側で強制していない
- 提起: 2026-08-13 / P2-13 実装中
- 内容: §7.5 は「忘れ物全体が分かる写真 1 枚を必須」、§8.1 は
  「写真 1 枚以上（安全上撮影困難な場合を除く）」と定める。
  P2-13 は写真の受け口を作ったが、**写真が 0 枚でも報告そのものは通る。**
- 理由: 写真の置き場（R2 のキー）が親の ID で決まるため、
  **報告を立ててからでないと写真を送れない。** 「写真が無ければ報告も
  作らない」にするには、① 1 リクエストに両方を載せる（`multipart` に
  JSON を混ぜる）か、② 一時領域へ先に置いて後で結び直す、のどちらかが要る。
  どちらも現場の再送（電波の悪い客室）で壊れ方が読みにくい。
- 影響: 写真ゼロの報告が残りうる。エラーコード `PHOTO_REQUIRED` は
  `contracts/report.ts` に用意してあるが、まだ誰も返していない。
- 暫定対応: **画面（M-13）が撮影を促す**（写真の欄を説明より前に置いた）。
  §8.1 の「安全上撮影困難な場合を除く」があるので、**不具合側は
  そもそも必須にできない。**
- 決める人: 忘れ物側だけ「写真が無い状態から `STORED` へ進めない」に
  するか（責任者が保管する時点では必ず現物がある）。W-09 / W-10 で
  「写真なし」を目立たせるだけで足りるかもしれない。

### #054 日報の和文フォントを R2 へ置く作業が済んでいない（**人間の作業**）
- 提起: 2026-08-13 / P2-14 実装中
- 内容: 日報 PDF に和文を出すには TTF が要る（DECISIONS #086）。
  実装は R2（`DOCUMENTS`）の `fonts/pk-jp-regular.ttf` を読むが、
  **オブジェクトそのものはまだ置かれていない。**
- 影響: **置かれるまで日報は 1 通も作られない。** コンシューマは
  `FONT_ASSET_MISSING` を返して retry し、`max_retries = 3` の後に
  そのメッセージは落ちる（ログに `daily-report-font-missing` が残る）。
  自動生成は毎日走るので、置いた翌日から出るようになる。
- 暫定対応: 無し。**空白の PDF を出すより出さないほうがよい**と判断した。
- やること（人間）:
  ```
  wrangler r2 object put pk-documents/fonts/pk-jp-regular.ttf --file <TTF>
  # preview / staging / production は bucket 名が違う（wrangler.toml）
  ```
  条件は「日本語のグリフを持ち、埋め込みが許諾されている TTF」。
  P0-02（Cloudflare リソース作成）と同じ性質の作業。
- 決める人: どのフォントを採るか（ライセンスの確認を含む）。

### #055 W-08（日報一覧）の画面が無い
- 提起: 2026-08-13 / P2-14 実装中
- 内容: §12.1 は `W-08 /app/p/[id]/reports/daily 日報一覧` を定め、
  §17 の Week 4 も «DailyReport / PDF» と «W-08» を並べる。
  P2-14 は **API（§14.4 の 5 経路）までを実装した**が、画面が無い。
  **P2-14 の完了条件 5 つに画面は含まれていない**（P2-11〜P2-13 で
  W-09 / W-10 を落としたのと同じ形）。
- 影響: いまは API を直接叩かないと日報に到達できない。§9.6 の
  「管理画面での閲覧」は満たせていない。ダウンロード（署名付き URL）と
  一覧・詳細の API は動く。
- 暫定対応: 無し。
- 決める人: W-08 / W-09 / W-10 をまとめて 1 task に起こすか、
  P2-16（P1 暫定機能の移行・削除）の後に PC 画面の task を追加するか。

### #056 §10 の指標を読む口が無い（API も画面も仕様に無い）
- 提起: 2026-08-13 / P2-15 実装中
- 内容: §10.1 は 7 指標を定めるが、**§12.1 に指標の画面が無く、
  §14 に指標の API も無い。** P2-15 の完了条件も「7 指標が算出される」
  だけで、経路を求めていない。そこで `packages/engine/src/metrics.ts`
  （純粋関数）までを実装し、API・画面は足していない。
- 影響: いま `computePropertyMetrics()` を呼ぶ本番コードは無い。
  読み手が現れるのは **PK-SPEC-P5 §7.1 の W-02（組織ダッシュボード）**で、
  そこは「`dailyPropertyRollup` から取得する。タスクテーブルへの直接集計を
  行わない」と定めている。**この 7 指標を rollup で賄うには列が足りない**
  （現在の列は `totalTasks` / `completedTasks` / `reworkTasks` /
  `totalMinutes` / `openIssues` / `findingsHigh` の 6 つで、初回検査合格
  件数・自己検査件数・検査待ち時間・SLA 超過件数・種別×客室タイプ別の
  内訳を持たない）。
- 暫定対応: engine の純粋関数として置いた。**入力は行の配列**なので、
  rollup から組んでも生の行から組んでも同じ関数を通せる。
- 決める人: P5 で W-02 を作るときに (a) `dailyPropertyRollup` へ列を足して
  日次で貯めるか、(b) 期間の行を読んで都度算出するかを決める。
  rollup は「§19.6 の定義そのまま。勝手に足さないこと」と書かれているので、
  (a) を採るなら P0-21 の設計判断を上書きすることになる。

### #057 移行の 2 経路に PC 画面が無い（§13 に画面の定義が無い）
- 提起: 2026-08-13 / P2-16 実装中
- 内容: §13.3 の残存タスク処理のために
  `GET /api/v1/inspections/stranded` と
  `POST /api/v1/tasks/:taskId/inspection/override` を足したが、
  **§12.1 に対応する画面が無い。** §13 は移行手順の節で、画面を定義しない。
- 影響: 施設責任者は API を直接叩くか、運用側が代行することになる。
  **移行のときにしか使わない経路**なので、常設の画面を作ると
  「検査せずに閉じる」ボタンが管理画面に残り続ける（§13.1 で消した
  一括承認と同じ見え方になる）。作らなかったのはそれも理由。
- 暫定対応: API まで。W-06（検査待ち）に混ぜていない。混ぜると
  日常の画面から検査を飛ばせるようになる。
- 決める人: P2-17 の現場検証で残存が実際に出るかを見てから。
  出るなら、移行専用の画面（常設しない）を 1 つ起こすか、
  運用手順（運営が API を叩く）にするかを決める。

### #059 アメニティの「使用あり／なし」と「個数」を品目ごとに分けていない
- 提起: 2026-08-13 / P3-04 実装中
- 内容: §12.4 は「アメニティの『使用あり／なし』と『個数』のどちらを
  既定とするか。品目により異なる」を未決としている。§4.2 のワイヤーは
  歯ブラシ・スリッパ・ミネラル水をステッパー、シャンプー類を
  「使用あり」のトグルで描いており、**どの品目がどちらかの一覧は無い。**
- 影響: M-05b は**全品目をステッパー（数値）で出している。** 0/1 で
  「使用あり／なし」を表せるので情報は失われないが、シャンプーのように
  個数を数える意味が薄い品目でも 2 タップ以上かかる。
- 暫定対応: 契約（`observationCountsSchema.amenitiesUsed`）の値を
  `number | boolean` にしてある。品目ごとの分類が決まっても、
  保存済みの行を読み替えずに画面だけを変えられる。
- 決める人: P3-13 の蓄積で、品目ごとに「0 と 1 以外の値がどれだけ出るか」を
  見てから決める。1 と 0 しか出ない品目はトグルにしてよい。

### #060 リネンの供給枚数（`suppliedQty`）を現場が入力しない
- 提起: 2026-08-13 / P3-06 実装中
- 内容: `linenRecord` は `collectedQty`（回収）と `suppliedQty`（供給）の
  2 列を持つ（§2.3）。**§4.3 のワイヤーには「回収した枚数」しか無い。**
- 影響: M-06 は `suppliedQty` を常に 0 で送る。列は残っているので、
  リネン回収担当が別途入力する形（§12.3 の未決事項）になったときに使える。
- 暫定対応: 画面に出していない。**0 が「供給していない」なのか
  「入力していない」なのかを区別できない**ので、P4 で `suppliedQty` を
  照合に使わないこと。
- 決める人: §12.3（リネン記録を清掃員が入力するか、回収担当が別途入力するか）
  が決まるとき。P5 のリネン費用の請求で必要になったら前倒しする。

### #061 `cupsUsed` と `extraFutonUsed` に対応する品目コードが無い
- 提起: 2026-08-13 / P3-09 実装中
- 内容: `roomObservation` は §2.1 のとおり `cupsUsed`（コップ）と
  `extraFutonUsed`（追加布団）の列を持つ。しかし §2.5 の品目コード一覧
  （リネン 9・アメニティ 10）に**この 2 つに当たるコードが無い。**
  一方 §6.2 の W-21 のワイヤーには「追加布団」の行が描かれている。
- 影響: ベースラインの集計キーは `itemCode` を含む（§5.2）ので、
  **この 2 項目はベースラインを持てない。** 観察記録には残るが、
  P4 の照合で使えない。§6.2 の画面例にある「追加布団」の行は出ない。
- 暫定対応: 品目コードを勝手に増やしていない。`ITEM_CODES` は
  `linenRecord` / `consumptionBaseline` / `baselineExclusionLog` の
  保存済みの値で、**一度使ったコードは変えられない**（schema の注記）。
  §2.5 が MUST で一覧を閉じている以上、こちらを正とした。
- 決める人: **P4 着手前。** 照合ルール（R001〜R014）が追加布団を扱うなら
  `EXTRA_FUTON` / `CUP` を §2.5 へ足す改訂が要る。足すなら
  `ITEM_CODES` への追加とベースラインの再計算だけで済む（列は既にある）。

### #062 ベースラインの集計ウィンドウを施設ごとに設定する場所が無い
- 提起: 2026-08-13 / P3-09 実装中
- 内容: §5.4 は「既定 90 日・最小 30 日・最大 180 日、季節性がある施設
  （リゾート等）は 365 日を選択可能」と定める。しかし §2.6 の
  `observationConfig` に**日数を持つ列が無い。**
- 影響: 週次バッチは全施設 90 日で走る。リゾート施設で「夏と冬で使用量が
  違う」ことをベースラインに織り込めない。
- 暫定対応: `apps/web/src/lib/baseline/window.ts` に定数
  （`DEFAULT_/MIN_/MAX_BASELINE_WINDOW_DAYS`）と範囲の検査を置き、
  メッセージ（`BaselineLearningMessage.windowDays`）に日数を持たせてある。
  **列を 1 つ足して W-20 に入力欄を作れば、それだけで効く形にしてある。**
- 決める人: P4 着手前、または季節性のある施設が実際に入るとき。
  列を足すなら `observation_config.baseline_window_days`（既定 90）。

### #064 入室記録（`roomAccessLog`）を登録する画面が仕様に無い
- 提起: 2026-08-14 / P4-10 実装中
- 内容: §2.3 は「誤検知を減らすため、業務上の入室を事前・事後に登録
  できるようにする」と定め、抑制条件（§4.1）もこの表を見る。しかし
  **§6 の画面一覧にも `ui-prototypes/` にも、登録する画面が無い。**
  W-07（§6.2）は「入室記録 登録なし」と参照するだけで、そこから
  登録できるとは書かれていない。
- 影響: API（`POST /api/v1/room-access-logs`）はあるが、**管理画面から
  登録する導線が無い。** 点検・修繕の予定を入れる運用が始められない。
- 暫定対応: 画面を推測で作っていない（CLAUDE.md §1.4）。API と
  リポジトリ・権限・テストまでを P4-10 で用意し、登録の導線は
  置き場所が決まってから足す形にしてある。
- 決める人: **P4-13（W-25 ルール設定）またはその前。** 置き場所の候補は
  ① W-03 客室ボードの客室メニュー（「いま入室した」を 1 タップで残す）
  ② W-25 ルール設定と並ぶ施設設定の 1 画面（予定を先に入れる）
  ③ W-07 差異詳細から「業務上の入室だった」として遡って足す。
  **③ は避けたい。** 差異を見てから抑制の根拠を作る導線になり、
  DECISIONS #115 で書けるロールを絞った意味が薄れる。

### #065 W-07 の「確信度の内訳」と「時系列」がプロトタイプにだけある
- 提起: 2026-08-14 / P4-07 実装中
- 内容: `ui-prototypes/owner/pkown-v3-B-findings-records.html`（05 差異の
  詳細）は、3 系統の比較に加えて**確信度の内訳**（「複数項目の一致 +38」
  のような寄与の分解）と**時系列**（前回チェックアウト → 清掃完了 →
  差異として記録）を持つ。**§6.2 の仕様にはどちらも無い。**
- 影響: 実装した W-07 は §6.2 のとおり「3 系統 ＋ 参考情報 ＋ 対応 ＋
  対応履歴」で、プロトタイプより情報量が少ない。
- 暫定対応: 仕様（§6.2 MUST）を正とした。CLAUDE.md §7 は
  「見た目の正は `ui-prototypes/`」としつつ、仕様書と矛盾する場合は
  PK-IMPL-CONTRACT → 仕様書の順で優先すると定めている。
  **確信度の内訳は engine が寄与を返していないと作れない**
  （`packages/engine` の `FindingDraft` は合計値だけを持つ）。
- 決める人: P4-08（誤検知率の検証）の後。内訳を出すなら
  `FindingDraft` に寄与の配列を足す改訂が要り、`auditFinding.evidence`
  の形も変わる（既存の差異は内訳を持たないままになる）。

### #066 R007 / R008 / R009 / R011 に条件の記述が無い
- 提起: 2026-08-14 / P4-12 実装中
- 内容: §3 が条件を書いているのは R001〜R006 と R010 / R013 / R014 の
  9 個。**残り 4 個は §3.1 の一覧（名称・重要度・必要系統）にしか無い。**

  | コード | 名称 | 決まっていないこと |
  |---|---|---|
  | R007 | ベッド数と人数の相違 | どちらがどれだけ上回れば差異か。客室タイプの定員を見るのか、`bedsUsed` と `guestCount` を比べるのか |
  | R008 | アメニティ消費の異常 | 基準値をどう当てるか。§2.5 の品目コードに `cupsUsed` / `extraFutonUsed` が無い（#061） |
  | R009 | リネン回収数の不足 | 「不足」の基準。回収数（`linenRecord`）と使用数（`roomObservation`）のどちらを見るか |
  | R011 | 清掃時間の著しい短さ | 「著しい」の閾値。標準時間（P1-02）に対する比率か、絶対値か |
- 影響: `RULES` に載るのは 10 個。**4 つは `ruleConfig` に設定を作っても動かない**
  （`findRule()` が `undefined` を返す）。W-25（P4-13）のルール設定画面には
  「未実装」として出すか、そもそも出さないかを決める必要がある。
- 暫定対応: 実装していない。R003（§3.4）に倣って `p90 + 1` を当てるのは
  推測になる（CLAUDE.md §1.4「推測で実装しない」）。
  **registry に 1 行足すだけで動く形は用意してある。**
  R012（写真未添付での完了）だけは名称が条件を言い切っており、
  閾値を決める余地が無いので実装した。
- 決める人: **P4-08（誤検知率の検証・人間が実施）の後。** 実データで
  R001〜R006 の誤検知率を見てからでないと、4 つの閾値も決められない
  （§実装順序が「まず 2 つ → 検証 → 残り」としているのと同じ理由）。
  R008 は #061（品目コードの欠落）も一緒に解く必要がある。

### #067 W-25 の画面番号が 2 つの仕様書で衝突している
- 提起: 2026-08-14 / P4-13 実装中
- 内容: PK-SPEC-P1 §10.1 の W-25 は「客室タイプ管理」（P1-24 で実装済み /
  `/app/settings/room-types`）。PK-SPEC-P4 §2.7 の W-25 は
  「ルール設定」（P4-13 / `/app/settings/rules`）。**同じ番号が別の画面。**
- 影響: 画面番号で会話すると取り違える。実装は経路が別なので実害は無い。
- 暫定対応: 両方を作り、それぞれのファイル冒頭に衝突を明記した。
  番号を勝手に振り直していない（仕様書の変更にあたる）。
- 決める人: 仕様書の版を上げるとき。どちらかを別の番号にするか、
  Phase ごとに番号空間を分ける（W1-25 / W4-25 のように）。

### #068 ルール設定の画面から閾値を編集できない
- 提起: 2026-08-14 / P4-13 実装中
- 内容: §2.7 の `ruleConfig.thresholds` はルールごとに鍵が違い、
  「engine が知っている形だけを入れること」とされている。**どのルールが
  どの鍵を読むのかを engine が公開していない**ため、画面に入力欄を出すと
  効かない鍵を自由に入れられる形になる。
- 影響: W-25 から触れるのは「有効・無効」と「重要度の上書き」の 2 つ。
  閾値の調整は `PATCH /api/v1/rule-configs/:ruleCode` から行う。
  P4-13 の完了条件「閾値を調整できる」は**API までの達成**。
- 暫定対応: 画面に自由入力欄を作っていない（CLAUDE.md §1.4）。
  API は閾値を受け取り、`rulesetHash` にも反映される。
- 決める人: P4-08 の後。`Rule` に「読む閾値の鍵と既定値」を持たせれば、
  画面はそれを見て入力欄を組み立てられる（engine 側の小さな追加で済む）。

### #069 `RECHECK` に対応する品目コードが §2.4 に無い
- 提起: 2026-08-14 / P5-04 実装中
- 内容: PK-SPEC-P5 §2.4 の品目コードは 10 個で、`CLEAN_CHECKOUT` /
  `CLEAN_STAYOVER` / `CLEAN_DEEP` / `CLEAN_COMMON` が清掃 4 種に対応する。
  一方 `TASK_TYPES` は 5 つあり、**`RECHECK`（再確認）に対応する行が無い。**
  §3.1 の除外にも挙がっていない（除外は `CANCELLED` / `BLOCKED` / 再清掃）。
- 影響: 再確認のタスクを請求に載せる根拠も、載せない根拠も無い。
- 暫定対応: 近い名前へ寄せていない（`CLEAN_CHECKOUT` にする、
  `EXTRA_REQUEST` にするのはどちらも推測）。§3.2 MUST「黙って落とさない」
  に合わせ、`ADJUSTMENT` の**¥0 明細＋ `ITEM_CODE_NOT_MAPPED` 警告**として
  計上する。金額は付かないが、件数は請求書に残り画面に警告が出る。
- 決める人: P5-06（PDF テンプレート）か、現場で再確認の有償・無償が
  決まったとき。有償なら品目コードを 1 つ足し、`ITEM_CODE_BY_TASK_TYPE`
  （packages/billing/src/vocabulary.ts）に 1 行足す。

### #070 「再清掃の有償設定」に対応する列が無い
- 提起: 2026-08-14 / P5-04 実装中
- 内容: §3.1 は再清掃（`ReworkCycle`）を「※ 有償設定の場合のみ計上」と
  するが、**その設定を持つ列が §2.1 の取引先にも §2.2 の料金設定にも無い。**
- 影響: 既定をどちらに倒すかで、取引先へ請求する金額が変わる。
- 暫定対応: `buildInvoiceDraft()` の `chargeRework` を**既定 `false`** にし、
  呼び出し側が明示したときだけ計上する。既定を `true` に倒すと、設定を
  書いていない取引先へ再清掃の代金を請求してしまう。除外した件数は
  `EXCLUDED` 警告（`detail: "REWORK_NOT_CHARGEABLE"`）で残る。
- 決める人: P5-05（月次締め）が集計を呼ぶとき。取引先に
  `chargeRework` 相当の列を足すのが素直だが、破壊的でないスキーマ追加に
  なるので、根拠（仕様の版上げ）を待つ。

### #071 取引先と施設の対応表が仕様に無い
- 提起: 2026-08-14 / P5-05 実装中
- 内容: §3.1 は集計対象を「期間内に `COMPLETED` となった CleaningTask」と
  するが、**どの施設のタスクを含めるかを決める表が §2 に無い。**
  `property` は `counterpartyId` を持たず、`counterparty` も施設の一覧を
  持たない。唯一その対応を持つのが `pricingRule.propertyId` で、
  §2.2 はその `null` を「取引先の全施設」と定めている。
- 影響: 1 組織が複数のホテルと取引する（§7.2 の清掃会社プラン）とき、
  請求書にどの施設の作業が載るかが決まらない。
- 暫定対応: `counterpartyPropertyScope()` が**料金設定から導く。**
  `propertyId` が null の行が 1 つでもあれば全施設、そうでなければ
  料金設定に現れる施設だけ。表を新設すると仕様の版上げが要る。
- 決める人: P5-07（請求書の発行）が対象タスクを引くとき。施設側に
  `counterpartyId` を持たせるのが素直だが、1 施設を複数の取引先で
  分担する運用があるかが分からない（§13 の未決事項 3 に近い）。

### #072 §9 の `request-review` に対応する状態が §2.8 に無い
- 提起: 2026-08-14 / P5-05 実装中
- 内容: §9 の API 一覧に `POST /billing-periods/:id/request-review` が
  あるが、**§2.8 の状態は 5 つ（`OPEN` / `REVIEWING` / `AGREED` /
  `INVOICED` / `CLOSED`）で「ホテルの確認待ち」に当たる値が無い。**
  §6.1 の図も「清掃会社が『ホテルへ確認依頼』」から先を
  （ホテル側に通知）と書くだけで、状態を増やしていない。
- 影響: 確認依頼を出したのか、まだ清掃会社が明細を直している最中なのか
  を状態から区別できない。
- 暫定対応: 状態を増やさない。`BILLING_PERIOD_ACTIONS` は
  `AGGREGATE` / `AGREE` / `REJECT` / `ISSUE_INVOICE` / `CLOSE` の 5 つ。
  確認依頼は**通知の送信**であって状態遷移ではない、と読んである。
- 決める人: P5-12（双方合意フロー）。差戻しコメントの表（§6.2 MUST）を
  設計するときに、依頼した事実をそこへ持たせるか状態を増やすかを決める。
- **決着（2026-08-14 / P5-12）: 状態を増やさない。** 確認依頼は
  `billing_period_review` に `REQUEST_REVIEW` の出来事として残す
  （docs/DECISIONS.md #128）。`POST /request-review` は `REVIEWING` の
  ときだけ 201 を返し、`billing_period.status` を触らない。

### #073 請求書の振込先（口座情報）に対応する列が無い
- 提起: 2026-08-14 / P5-06 実装中
- 内容: §8.1 の請求書の見本に「お振込先 サンプル銀行 渋谷支店 普通 1234567
  サンプルセイソウ(カ」の節があるが、**`organizationTaxProfile` は
  登録番号・住所・電話までで口座を持たない**（§2 のどの表にも無い）。
- 影響: 振込先の無い請求書が出る。取引先は入金先が分からない。
- 暫定対応: `InvoicePayload.bankAccountText` を `string | null` にし、
  発行時のスナップショット（`issuerSnapshot.bankAccountText`）から読む。
  **`null` のときテンプレートはその節ごと出さない。** 空欄の枠を載せると
  「振込先が空欄の請求書」という事故になる。
- 決める人: P5-07（発行）がスナップショットを固定するとき。
  `organizationTaxProfile` に列を足すのが素直（後方互換の追加）だが、
  口座を複数持つ運用・取引先ごとに振込先を変える運用があるかが分からない。
  仕様の版上げを待つ。

### #074 §10.6 の「双方が AGREED にしないと発行できない設定」に列が無い
- 提起: 2026-08-14 / P5-07 実装中
- 内容: §10.6 は「双方が AGREED にしないと請求書を発行できない**設定が
  可能**」と書くが、その設定を持つ列が §2.1 の取引先にも §2.8 にも無い。
  一方 §6.1 の本文と `billing_period` の注記は「**同意していなくても
  請求はできる**」と書いており、既定をどちらに倒すかが決まらない。
- 影響: 請求書を出せるタイミングが変わる。
- 暫定対応: **常に `AGREED` 必須**（人間の判断 / A 案）。マージ済みの
  状態機械（`evaluateBillingPeriodTransition()`）が既にそう動いており、
  §0.2 の出荷判定「同じ明細を見て相違なく合意できる」とも噛み合う。
  設定で緩められるようにするのは列ができてから。
- 決める人: 設定列を足すかどうかは P5-12（双方合意フロー）。
  **合意の口が P5-12 にしか無いので、それまで現場では請求書を出せない。**

### #075 帳票メールの差出人が組織ごとに設定できない
- 提起: 2026-08-14 / P5-07 実装中
- 内容: §4.1 の ⑩ は Resend で送るとだけ書き、**差出人（From）を持つ列が
  §2 に無い。** 取引先から見ると、請求書が ProofKeeping のドメインから
  届くことになる。
- 影響: 清掃会社の名前で送りたい要望が出うる（自社ドメインの検証が要る）。
- 暫定対応: `RESEND_FROM_ADDRESS`（wrangler.toml の `[vars]`）で
  環境ごとに 1 つ。組織ごとには変えられない。
- 決める人: P5-10（送付ログと bounce 処理）。組織ごとの差出人を許すなら
  ドメイン検証の状態を持つ表が要る（Resend の domains API）。

### #076 入金（Payment）を置く表が無い
- 提起: 2026-08-14 / P5-08 実装中
- 内容: §4.2 の ① は「Payment を記録、Invoice を PAID に」と書くが、
  **§2 に `payment` 表が無い。** `invoice` にも入金額の列が無く、
  あるのは `status` と `paidAt` だけ。一方 `INVOICE_STATUSES` には
  `PARTIALLY_PAID` があり、一部入金を表せる状態だけが用意されている。
- 影響: 一部入金・複数回の入金を記録できない。入金の履歴も残らない。
- 暫定対応: **全額入金だけを扱う。** 請求額と一致しない入金は
  409（`PARTIAL_PAYMENT_NOT_SUPPORTED`）で断る。**黙って全額として
  記録しない。** 領収書（§2.6）が `receivedAmount` / `receivedDate` /
  `paymentMethod` を持つので、全額入金の事実はそこに残る。
- 決める人: P5-09（訂正）または P5-11。`payment` 表を足すのが素直だが、
  §2 に無いものを新設すると仕様の版上げが要る。`PARTIALLY_PAID` が
  語彙にある以上、いずれ必要になる。

### #077 Resend の webhook がタグとヘッダのどちらを返すか確かめていない
- 提起: 2026-08-14 / P5-10 実装中
- 内容: webhook は「どのテナントの送付か」を知らない。一方こちらは
  シャードを引くために `organizationId` が要り、**全シャード走査は
  禁止**（architecture.md §3）。そこで**送付ログの ID を送信時に
  `tags` と `headers` の両方へ載せ**、webhook が返した値から
  `orgShortId` を取り出して `lookupOrganizationId()` で引いている。
  ID は自己記述（`{orgShortId}__dlv_{ulid}`）なので新しい表が要らない。
- 影響: **Resend が実際にどちらも返さなければ、bounce を送付ログへ
  写せない**（イベントは 200 で受け取るが、行が更新されない）。
- 暫定対応: タグとヘッダの両方を送り、受信側は両方を見る
  （先に見つかったほうを使う）。どちらも無ければ ack して落とし、
  `webhook-unreadable` としてログに出す。
- 決める人: **実機で 1 通送って webhook の payload を見るだけで決まる**
  （人間の作業）。返らない場合は、Resend の `email_id` →
  `documentDelivery.providerMessageId` で引ける全局の索引が要る
  （`org_directory` には業務データを置けないので別の表になる）。

### #078 §6.1 の「（ホテル側に通知）」に対応する送信経路が無い
- 提起: 2026-08-14 / P5-12 実装中
- 内容: §6.1 は確認依頼の先を「（ホテル側に通知）」と書くが、**帳票以外の
  メールを送る経路が無い。** `documentDelivery`（§2.7）の `docType` は
  `INVOICE` / `RECEIPT` / `DAILY_REPORT` / `AUDIT_REPORT` の 4 つで、
  「確認依頼」に当たる値が無い。件名・本文のテンプレートも §8 に無い。
- 影響: `POST /billing-periods/:id/request-review` は**履歴に残すだけで、
  取引先には何も届かない。** 清掃会社が別の手段（電話・メール）で
  伝えることになる。
- 暫定対応: 送らない。依頼した事実と、そのとき見えていた明細を
  `billing_period_review` に残す（docs/DECISIONS.md #128）。**送ったことに
  しない**（送っていないのに送付ログが立つほうが害が大きい）。
- 決める人: P6（外部連携）か、通知を足す task。`docType` を増やすか、
  帳票の送付ログとは別の通知ログを持つかを決める必要がある。
  取引先の宛先そのものは `counterparty.billingEmail` に既にある。

### #079 §10.6 の「AGREED 必須の設定」は列が無いまま常に必須にしてある
- 提起: 2026-08-14 / P5-12 実装中
- 内容: P5-12 の完了条件に「**AGREED にしないと発行できない設定が可能**」
  とあるが、その設定を持つ列は §2.1 の取引先にも §2.8 にも無い（#074）。
  #074 は人間の判断で「常に必須」（A 案）に倒してある。
- 影響: 取引先ごとに「合意を待たずに請求してよい」運用を選べない。
  合意の口（`POST /agree`）が P5-12 で入ったので、**発行が止まる状態は
  解消している**（それまでは `issue-and-send` が必ず 409 だった）。
- 暫定対応: **常に `AGREED` 必須のまま。** 列を足すのは仕様の版上げが
  要る（推測で列を作らない / CLAUDE.md §1-4）。設定を可能にすること
  自体が §0.2 の出荷判定「同じ明細を見て相違なく合意できる」を弱める
  ので、必要になった時点で理由と一緒に決めるほうがよい。
- 決める人: 仕様の版上げ。`counterparty.requiresAgreementBeforeInvoice`
  （boolean・既定 true）を足すのが素直。
