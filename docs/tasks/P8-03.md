# P8-03 シフトと当日の割当

**Phase**: 8（起票 2026-08-20 / DECISIONS #219）
**仕様**: `docs/PK-SPEC-P8.md` §1.5 / `ui-prototypes/ops/pkops-A-daily-quality.html` 02 シフトと割当
**依存**: P8-01
**見積**: 2日
**ステータス**: 実装済み（2026-08-20）

## 読むべきルール
`.claude/rules/ui-writing.md` §2 / `docs/PK-IMPL-CONTRACT.md` INV-06 / INV-09

## 背景
**プロトタイプ 02 に週間シフトのグリッドは無い**（DECISIONS #221）。
あるのは 3 つ。

1. KPI 4 枚（出勤予定 26 名 / 出勤済み 24 名 / 欠勤 2 名 / 未割当 0 室）
2. 表「👥 本日の割当」（担当者 / 施設 / 割当 / 完了 / 進捗 / 状態）
3. カード「📅 今週のシフト」＝**出勤者数の棒グラフ 1 本**（月〜日）

仕様 §1.5 の「スタッフ × 曜日のグリッド」「必要人数の行」は**作らない。**

## やること

### `shiftPlan` テーブル
仕様 §1.5 の定義どおり（`staffProfileId` × `businessDate` で一意）。
`shiftType` = `WORK` / `OFF` / `PAID_LEAVE` / `SICK` / `TRAINING`。

### KPI の出し方（**`attendance` を作らない**）
- 出勤予定 = その業務日の `shiftType = WORK` の人数
- 出勤済み = その業務日に**タスクを 1 件以上開始した**人数
- 欠勤 = 出勤予定 − 出勤済み
- 未割当 = 当日タスクのうち担当者未設定の室数

### 画面 `/app/shifts`
- 日付セレクト＋「自動割当」「保存」（プロトタイプのヘッダーどおり）
- 本日の割当の表。カード見出しの右に **「担当者名は運営管理者のみ閲覧できます」**
- 今週のシフト＝棒グラフ（出勤者数）＋脚注「登録スタッフ N 名 / 週平均 N 名」
- カード「⚙️ 自動割当のルール」5 行（**上限室数 16 室・連続勤務 5 日は数値表示のみ。
  ここから変更できる形にしない** — 上限の変更はプラットフォーム側 PF-14 の
  「運用（変更可）」で承認 2 名を要する）

### 前週の複製
仕様 §1.5 MUST。「清掃会社は固定シフトが多い」。**冪等**にする（3 回押しても同じ）。

## やらないこと
- **週間シフトのグリッド**（プロトタイプに無い）
- **必要人数の自動算出行**（同上。近いのは注記の文章 1 本だけ）
- 出勤打刻（DECISIONS #221）
- シフト希望の提出（プラットフォーム 08 の要望一覧で「検討中」の段階）

## 完了条件
- [x] シフト未登録でも P1-14 の自動配分が従来どおり動く（仕様 §1.5 MUST。
      配分はシフトを読まない — シフトは予定の表で、当日の実際を縛らない）
- [x] 前週の複製が冪等（`onConflictDoNothing`。**上書きしない** — 複製後に
      手で直したシフトを、もう一度押した複製が黙って戻さない）
- [x] 担当者名が `ORG_ADMIN` 以外に出ない（門は `shift.manage` =
      OWNER / ORG_ADMIN のみ / OPEN_QUESTIONS #112）
- [x] 出勤済みの件数が**タスクの開始から**出ている（`attendance` テーブルが無い。
      `staffPii.spec` / スキーマに表が無いことが証明）
- [x] テナント越境テスト 4 パターン（`tests/tenant-isolation/shift.spec.ts`）

## 作業ログ

- 2026-08-20 実装。`shift_plan` は**仕様の `staffProfileId` ではなく
  `membershipId`** で人を指す（タスクの担当・支払と同じキー / 表の
  docstring に理由）。入力は日単位（区分＋施設）で、**週間グリッドも
  必要人数の行も「自動割当」ボタンも無い**（プロトタイプに無い /
  W-04 と二重に押せる形にしない）。時刻（startAt / endAt）は列だけ
  置き、入力 UI は付けていない（プロトタイプの日次画面に時刻が無い）。
- 「今週のシフト」は出勤者数の棒グラフ（`pk-bars` を流用）。
- **migration 0029 に P8-01 / P8-02 の分も含まれている。** #132 / #134 が
  `pnpm db:generate` を忘れており、スキーマと migration がずれていた。
  この PR でまとめて生成した（すべて追加のみ・後方互換）。

## 作業ログ
（Claude Code がここに追記する）
