# P2-02 検査ポリシーと抽出ロジック

**Phase**: 2
**仕様**: docs/PK-SPEC-P2.md §2
**依存**: P2-01
**見積**: 1日
**ステータス**: 完了（2026-08-13）

## 読むべきルール
（CLAUDE.md のみ）

## やること
ALL / SAMPLE / NONE の 3 モード。

## 完了条件
- [x] 3 モードが動く
- [x] 必須検査対象（当日IN・前回差戻し・新人・不具合あり）が必ず選ばれる
- [x] **抽出対象かどうかが清掃完了前にスタッフへ見えない**

## 作業ログ

2026-08-13。

- `packages/engine/src/inspectionSampling.ts`（新規）
  `decideInspection()` / `isNewStaff()` / `policyFromLegacyFlag()`。
  **乱数と現在時刻は引数**（DECISIONS #062）。31 件のテスト。
- `packages/db/src/repositories/inspectionPolicy.ts`（新規）。
  **行が無いことに意味がある。** 読み取りのついでに既定行を作らない。
- `apps/web/src/lib/task/inspectionDecision.ts`（新規）。材料を集める側。
  `ALL` / `NONE` では材料を引かない（§15 の p95 に効く）。
- `runTransition()` の `complete` 分岐から 1 か所だけ呼ぶ（DECISIONS #061）。

**完了条件について**
- 「不具合あり」と「重点客室」は**規則を engine に実装してテストしてあるが、
  参照する表・列がまだ無い**（OPEN_QUESTIONS #043）。呼び出し側が `false` を
  渡している。表ができた task が 2 行差し替える。
- 「完了前に見えない」は応答の設計ではなく**決定の時点**で守っている。
  `start` の経路が `property_inspection_policy` を 1 度も引かないことを
  `tasks.spec.ts` が SQL で見ている。

