# P2-01 スキーマ: 検査・証跡

**Phase**: 2
**仕様**: docs/PK-SPEC-P2.md §3
**依存**: P1 完了
**見積**: 1日
**ステータス**: 完了（2026-08-13）

## 読むべきルール
architecture.md

## やること
`propertyInspectionPolicy`, `inspection`, `inspectionItemResult`,
`inspectionPhoto`, `reworkCycle`, `evidenceSnapshot`

## 完了条件
- [x] `evidenceSnapshot` に UPDATE/DELETE API がない
- [x] 越境テストが全テーブルにある

## 作業ログ

2026-08-13。6 表と `cleaningTask` の 7 列を足した。

- `packages/db/src/schema/inspection.ts`（新規）
  `propertyInspectionPolicy` / `inspection` / `inspectionItemResult` /
  `inspectionPhoto` / `reworkCycle` / `evidenceSnapshot`。
- `schema/task.ts` に §3.1 の 7 列（`inspectionRequired` ほか）。
- migration `0007_p2_01_inspection.sql`。**追加のみ**（architecture.md §6）。
- entityPrefix を 4 つ追加（DECISIONS #059）。

**仕様との差:** `inspectionItemResult` / `inspectionPhoto` に
`organizationId` と `propertyId` を足した（DECISIONS #060）。
親を辿らないと組織が分からない表を作らないため。

**完了条件について**
- `evidenceSnapshot` の UPDATE / DELETE が無いことは、doc コメントではなく
  `repositories.spec.ts`「証跡スナップショットの不変性」がソースを走査して
  固定している（drizzle の呼び出しと生 SQL の両方）。
- 越境テストは `property_inspection_policy` に実体がある。残る 5 表は
  リポジトリ関数を作る task（P2-04 / P2-07 / P2-08）が spec を足す。
  `_template.spec.ts` の `UNCOVERED_TABLES` に理由付きで宣言済み
  （**黙って抜けない形**にしてある）。

