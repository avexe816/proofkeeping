# P3-03 M-05 入室時の記録 ★UX が最重要

**Phase**: 3
**仕様**: docs/PK-SPEC-P3.md §4.1
**依存**: P3-02
**見積**: 2日
**ステータス**: 完了

## 読むべきルール
ui-writing.md §3-4

## やること
start 直後に全画面表示。15 秒で終わること。

## 完了条件
- [x] `start` の直後に全画面で表示される
- [x] 既定値がプリセットされ、1 タップで確定できる
- [x] **数値入力でキーボードが出ない**（ステッパーのみ）
- [x] ボタンが 56px 以上
- [x] **「今回は記録しない」がある**
- [x] `inputDurationMs` が記録される
- [ ] 入力所要時間の中央値が 20 秒以内（実測）
- [x] 「不審」「異常」等の語を使っていない

## 作業ログ
- 2026-08-13 完了。`/m/task/:taskId/observation`（M-05）。M-02 の「開始」を
  押すと自動で開く（§3.2）。**§3.2 の確認画面は作らず、この 1 画面に
  「今回は記録しない」を置いた**（DECISIONS #098）。挟むと 1 タップで
  確定できなくなる（§1.2 MUST）。
- 数値入力は `input` を 1 つも置かず、ステッパーと選択ボタンだけ
  （`pk-m-step__button` は 56px 角）。既定値は `GET` の応答に同梱される
  （DECISIONS #097）。既定値のまま確定すると `usedDefaults = true`。
- `inputDurationMs` は画面が開いてから確定するまでの実測。
  **中央値 20 秒以内の確認は実測が要る**（§0.3 / P3-13）。
