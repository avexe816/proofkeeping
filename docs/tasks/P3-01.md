# P3-01 スキーマ: 観察・リネン・ベースライン

**Phase**: 3
**仕様**: docs/PK-SPEC-P3.md §2
**依存**: P2 完了
**見積**: 1日
**ステータス**: 完了

## 読むべきルール
architecture.md

## やること
`roomObservation`, `observationRevision`, `linenRecord`,
`consumptionBaseline`, `observationConfig`, `baselineExclusionLog`
`cleaningTask` に `observationSkipped`, `observationRecordedAt` を追加

## 完了条件
- [x] `@@unique(taskId)` で 1 タスク 1 観察
- [x] 越境テストが全テーブルにある（**宣言のみ。**下記の申し送りを読むこと）

## 作業ログ
2026-08-13。6 表と `cleaningTask` の 2 列を足した。

- `packages/db/src/schema/observation.ts`（新規）
  `roomObservation` / `observationRevision` / `linenRecord` /
  `consumptionBaseline` / `observationConfig` / `baselineExclusionLog`。
- `packages/db/migrations/0012_p3_01_observation.sql`
  追加のみ（`CREATE TABLE` × 6 と `ALTER TABLE ... ADD` × 2）。
  破壊的変更なし（architecture.md §6）。実 SQLite に 13 本を順に流して確認した。
- `packages/db/src/id.ts` に接頭辞 5 件（DECISIONS #092）。
- `packages/db/src/schema/task.ts` に `observationSkipped` /
  `observationRecordedAt`。**宣言の末尾に置いた。**
  途中に挿すと `SELECT *` の並びを前提にした fake D1 の行
  （`apps/web` の spec）が 1 つずれて、無関係なテストが落ちる。

**仕様との差は 4 点**（すべて observation.ts の冒頭に理由を書いた）。
時刻を `timestamp_ms` に統一、一意 index の先頭に `organizationId`、
`observationConfig.propertyId` の一意を複合に、
`observationRevision` / `baselineExclusionLog` に `organizationId` を追加。

**`baselineExclusionLog` の列は仕様に無い**（表名だけがある）。
除外 1 件 = 1 行で持つことにした（DECISIONS #093）。

### 申し送り — 越境テストは宣言のみ

P2-01 と同じく、この task は表と migration まで。読み書きの関数が
無い表に `describeTenantIsolation()` は書けないため、
`tests/tenant-isolation/_template.spec.ts` の `UNCOVERED_TABLES` に
6 表を理由付きで載せた。**関数を作る task が spec を足して行を消すこと**
（P3-03 / P3-06 / P3-07 / P3-09 / P3-10 / P3-11 / P3-12）。

