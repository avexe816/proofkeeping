# P5-12 双方合意フロー

**Phase**: 5
**仕様**: docs/PK-SPEC-P5.md §6
**依存**: P5-05
**見積**: 2日
**ステータス**: 完了

## 読むべきルール
（CLAUDE.md のみ）

## やること
清掃会社とホテルの明細確認。

## 完了条件
- [x] 明細行単位で差戻しコメントが付けられる
- [x] 修正履歴がすべて残る
- [x] AGREED にしないと発行できない設定が可能
      （**常に必須**として実装。設定を持つ列は仕様に無い / OPEN_QUESTIONS #074・#079）

## 作業ログ

### 置いたもの

| 口 | 何をするか |
|---|---|
| `GET  /billing-periods/:id/lines` | 発行前の明細（`lineKey` つき）と警告・合計 |
| `POST /billing-periods/:id/request-review` | ホテルへの確認依頼。**状態を変えない** |
| `POST /billing-periods/:id/agree` | `REVIEWING → AGREED`。合意時の明細を固定 |
| `POST /billing-periods/:id/reject` | 差戻し。**コメント必須**。合意を取り消す |
| `GET  /billing-periods/:id/reviews` | 履歴（古い順）。**追記だけ** |

- 表: `billing_period_review`（`bprv`）。migration
  `0015_p5_12_billing_period_review.sql`。UPDATE / DELETE の口を作っていない。
- `@pk/billing` に `billingLineKeyOf()` を足し、`DraftInvoiceLine.lineKey` を
  追加した。集計の粒度（施設 × 清掃種別 × 客室タイプ）そのもの。
- 明細の組み立てを `apps/web/src/lib/billing/draft.ts` へ切り出し、
  発行（P5-07）と合意が同じ `buildPeriodDraft()` を通るようにした。

### 止まらずに判断したこと

- **DECISIONS #127** 履歴を新表で持つ（`billingPeriod` に列を足すと
  2 回目の差戻しが 1 回目を上書きし、§6.2 MUST が成り立たない）
- **DECISIONS #128** `request-review` は状態を増やさず出来事として残す
  （OPEN_QUESTIONS #072 の決着）
- **DECISIONS #129** 明細の組み立ては発行と合意で 1 か所

### 積み残し

- **OPEN_QUESTIONS #078** 確認依頼の通知を送る経路が無い（`docType` に
  値が無く、本文テンプレートも仕様に無い）。**履歴に残すだけ。**
- **OPEN_QUESTIONS #079** 「AGREED 必須の設定」は列が無く、常に必須のまま。
- 明細行 → タスク一覧 → 証跡（§6.3）は **P5-13**。`lines` は
  タスク**件数**までを返し、ID の一覧は返していない。
