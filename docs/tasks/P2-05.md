# P2-05 M-08 検査待ち一覧

**Phase**: 2
**仕様**: docs/PK-SPEC-P2.md §11.2
**依存**: P2-04
**見積**: 1日
**ステータス**: 完了（2026-08-13）

## 読むべきルール
ui-writing.md §3

## やること
緊急度順の並び替え。

## 完了条件
- [x] 緊急（チェックインまで 30 分未満）が最上位
      → 規則は `sortInspectionQueue()`（engine・22 テスト）。
        **ただしチェックイン予定時刻の列がまだ無く、実データでは
        この段が常に空**（OPEN_QUESTIONS #045）。列ができたら
        `lib/inspection/waiting.ts` の 1 行を差すだけで効く。
- [x] SLA 超過がオレンジ表示（`--warn`。赤は緊急だけ / DECISIONS #069）

## 作業ログ

### 2026-08-13

- engine `inspectionQueue.ts`: §11.2 の 4 段（緊急 → SLA 超過 → 再検査 →
  完了の古い順）。**段を跨いだ入れ替えをしない。**
- `GET /api/v1/inspections/waiting?propertyId=&businessDate=`。
  **`/:inspectionId` より前に登録**（`waiting` が ID として吸われる）。
- 画面 `/m/inspections`。30 秒自動更新＋手動更新。「検査する」は 1 タップで
  `POST /tasks/:id/inspection/start` → M-09 へ。
- 下部タブに「検査」を追加。**`CLEANER` には出さない**（DECISIONS #070）。

**止まらずに判断したこと**
- #068 検査の記録をオフラインキューに載せない
- #069 緊急だけ赤、SLA 超過はオレンジ
- #070 検査タブをロールで出し分ける
- OPEN_QUESTIONS #045（チェックイン時刻の列が無い）
- OPEN_QUESTIONS #046（担当者名を出さない側を採った）
