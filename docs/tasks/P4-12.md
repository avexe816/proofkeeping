# P4-12 R007 〜 R014 実装

**Phase**: 4
**仕様**: docs/PK-SPEC-P4.md §3.8-3.10
**依存**: P4-11
**見積**: 2.5日
**ステータス**: 完了（2026-08-14）

## 読むべきルール
testing.md §3

## やること
残りのルールを追加する。

## 完了条件
- [x] R010 に「業務手順の見直しが必要な可能性」の文言がある
- [x] R002 と R001 の同時発生が統合される

## 作業ログ

### 2026-08-14
- R002（施錠解除と稼働記録の不一致 / §3.3）・R010（客室ステータスの
  手動上書き頻発 / §3.8）・R012（写真未添付での完了）・R013（深夜帯の
  施錠解除 / §3.9）・R014（稼働記録の事後変更 / §3.10）を追加。
  registry は 10 個になった。
- **R002 は task の表題（R007〜R014）に入っていないが、ここで実装した。**
  完了条件が「R002 と R001 の同時発生が統合される」を求めており、
  R002 はどの task にも割り当てられていなかった（P4-04 は R001 / R006 の
  2 つだけ）。
- §3.3 MUST の統合は `evaluate()` の `mergeR001IntoR002()`。
  **ルールどうしを参照させない**（registry の並びに意味が生まれ、
  順番を変えただけで結果が変わる形になる）。両方の `matchedSignals` を
  残し、確信度に +25（§3.3 の「観察でも使用痕跡があるなら」）。
- §3.8 MUST の文言は `R010.summary` に固定で入れてある。**消さないこと**
  （W-07 が `summary` をそのまま出す）。氏名は根拠に入れない
  （security.md §5 / INV-07）。
- 深夜帯（0:00-5:00）は施設の地域時刻。**engine は時差を解けない**ので、
  `SignalFact.localHour` を呼び出し側が変換して渡す。
- R010 / R014 の根拠は監査ログにしか無い。`listAuditLogs()` を足した
  （**期間と操作種別が必須**なので「全部読む」呼び出しは書けない）。
- R012 のために `task.photoCount` を実データにした（P4-05 は 0 固定だった）。
- テストは R002 17 件・R010 13 件・R012 11 件・R013 15 件・R014 11 件・
  統合 12 件。

## 残っていること
- **R007 / R008 / R009 / R011 は実装していない。** §3 に条件の記述が無く、
  閾値を推測することになる（OPEN_QUESTIONS #066）。
  registry に 1 行足すだけで動く形は用意してある。

## 止まらずに判断したこと
- 確信度の式が仕様に無い R012 / R013 / R014 の値（DECISIONS #116）。
- `engineVersion` を 1.1 へ上げた（DECISIONS #117）。
