# P4-01 スキーマ: 照合

**Phase**: 4
**仕様**: docs/PK-SPEC-P4.md §2
**依存**: P3-13
**見積**: 1.5日
**ステータス**: 完了

## 読むべきルール
security.md §3

## やること
`occupancySnapshot`, `physicalSignal`, `roomAccessLog`,
`reconciliationRun`, `auditFinding`, `detectionFeedback`, `ruleConfig`

## 完了条件
- [x] 宿泊者の氏名・連絡先カラムが**存在しない**
- [x] 越境テストが全テーブルにある

## 作業ログ

**7 表を `packages/db/src/schema/reconciliation.ts` に置いた。**
`occupancySnapshot` / `physicalSignal` / `roomAccessLog` /
`reconciliationRun` / `auditFinding` / `detectionFeedback` / `ruleConfig`。
migration は `0013_p4_01_reconciliation.sql`（**追加のみ**）。実 SQLite に
14 本を順に流して確認した。

**宿泊者の情報を持つ列は 1 つも無い**（§2.1 MUST）。`occupancy_snapshot` の
列名を機械的に走査して `name` / `phone` / `email` / `address` / `passport` /
`card` を含む列が 0 件であることを確認済み。`roomAccessLog.actorName` は
**立ち入った従業員**で、宿泊者ではない（security.md §5 の扱い）。

**仕様との差は 2 つ**（P3-01 と同じ判断・schema のコメントに明記）。
① 時刻は全て `timestamp_ms`。② 一意索引・索引の先頭に `organizationId` を
足した。仕様の `uq_occ` / `uq_run` / `uq_finding` は組織をまたいで一意になり、
同居する別組織の行と衝突する（architecture.md §2 第1層）。

**接頭辞を 5 つ足した**（DECISIONS #105）。`occ` / `sig` / `racc` / `dfb` /
`rcfg`。`find` / `run` は architecture.md §2 に定義済み。
`roomAccessLog` を機械的に `room` にすると客室マスタと衝突するので `racc`。

**越境テストは `occupancy_snapshot` だけカバー**（P4-02 が関数を作った）。
残る 6 表は `UNCOVERED_TABLES` に理由付きで載せてある。**関数を作る task
（P4-05 / P4-07 / P4-10 / P4-13）が spec を足して行を消すこと。**
