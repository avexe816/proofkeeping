# P1-05 ステータス遷移 API

**Phase**: 1
**仕様**: docs/PK-SPEC-P1.md §5
**依存**: P1-01
**見積**: 1.5日
**ステータス**: 完了

## 読むべきルール
（CLAUDE.md のみ）

## やること
assign/start/pause/resume/complete/block/cancel を実装する。

## 完了条件
- [ ] 6 操作すべてが実装されている
- [ ] 必須チェック未完了で `complete` が拒否される
- [ ] 写真必須項目に写真がないと拒否される
- [ ] `Idempotency-Key` に対応
- [ ] 中断を 3 回挟んでも `actualMinutes` が正しい

## 作業ログ

### 2026-08-12 — 完了

**やったこと**

- 状態機械を `packages/engine/src/taskStatus.ts` に**純粋関数**として置いた
  （遷移表・再送の扱い・理由コード必須の判定）。
- 作業時間の集計は `packages/engine/src/taskTime.ts`。
- 実行は `apps/web/src/lib/task/transition.ts`、API は
  `POST /api/v1/tasks/:taskId/:action`。

**完了条件**

- [x] 6 操作すべてが実装されている
      → **8 操作を実装した。** task の やること は 7 つ（assign/start/pause/resume/
        complete/block/cancel）を挙げ、完了条件は「6 操作」と書いていて本数が
        食い違う。§5.1 の状態機械を閉じるには `unblock` も要る
        （無いと `BLOCKED` が終端になり、入室できるようになった客室を進められない）。
- [x] 必須チェック未完了で `complete` が拒否される → `CHECKLIST_INCOMPLETE` + 項目 ID 一覧。
- [x] 写真必須項目に写真がないと拒否される → `PHOTO_REQUIRED` + 項目 ID 一覧。
- [x] `Idempotency-Key` に対応 → `taskTimeLog.idempotencyKey` の一意列（DECISIONS #034）。
      再送は 409 ではなく **200 + `unchanged: true`**（§8.2 の「409 は成功扱い」を
      409 を作らずに満たす）。
- [x] 中断を 3 回挟んでも `actualMinutes` が正しい → `taskTime.spec.ts`。
      並び替え・二重 START・巻き戻しでも落ちない。

**判断したこと**

1. `complete` の検証は**時間ログを書く前**に行う。逆順にすると、拒否された
   完了操作の時間ログだけが残る。
2. `applyTransition()` は `expectedStatus` を条件に含める（楽観的排他）。
   並列に着地した 2 つ目は 0 行更新になり、`unchanged` として返る。
3. **`start` / `pause` / `resume` を監査ログに書かない**（INV-07）。
   記録するのは `complete` / `assign`（§4.2）/ `cancel`（破壊的操作）/
   `block`（§5.3 が理由を必須とする）の 4 つ。
4. `COULD_NOT` は完了を妨げない。妨げると清掃員が事実と違う `DONE` を押す。

