# PF-03 サービス稼働（画面 01）

**系列**: PF（起票 2026-08-20） **依存**: PF-01 **見積**: 1.5日 **ステータス**: ⏳ 人間待ち（出す元の無い指標 / OPEN_QUESTIONS #114）
**仕様**: `ui-prototypes/platform/pkplat-A-status-tenants.html` 01 / route `/plat/status`

## やること
- KPI 5: 稼働率（30日）/ API 応答 p95 / 同期キュー / 同期の失敗 / 写真ストレージ
- 表「📡 コンポーネントの状態」（コンポーネント / 状態 / 応答 p95 / 直近24時間 / エラー率）
- 表「直近の事象履歴」（日時 / 内容 / 状態）
- カード「📱 オフライン同期の状況（全テナント）」4 行
- 「📊 24時間のリクエスト量」棒グラフ

## 逐語で置く注記
> 競合が起きても清掃員の記録は削除しません。採用されなかった記録も conflictLog に保持しています。

> 清掃業務は早朝から午前中に集中します。深夜帯のメンテナンスウィンドウは 02:00〜04:00 を基本とします。

**↑ この注記は 03:00〜04:00 へ修正した**（DECISIONS #239 / オーナー判断。
境界は **03:00 以上 04:00 未満**）。PF-14 の「運用（変更可）」の既定に
合わせてある。**逐語で置くのは上の文ではなく、修正後の文。**

## やらないこと
- **数字を捏造しない。** 出す元が無い指標は列ごと出さない（P7-19 と同じ判断 /
  OPEN_QUESTIONS #107）。稼働率・p95 の実測が無いなら、まずどこから採るかを
  OPEN_QUESTIONS へ書いて止まる。**0 で埋めない。**

## 完了条件
- [x] 出す元のある指標だけが並んでいる（無い指標は列ごと無い）
- [x] シャード番号・内部ホスト名が出ない
- [x] 事象履歴が時系列で、書き込みの口が無い
      — **表そのものを置いていない**（出す元が無い / #114）。書き込みの口も無い

## 作業ログ

### 2026-08-20 出せるところまで実装し、残りは #114 で止めた

**この task の「やらないこと」に従って止めた。**
> 稼働率・p95 の実測が無いなら、まずどこから採るかを OPEN_QUESTIONS へ
> 書いて止まる。**0 で埋めない。**

出す元を数えたところ、**プロトタイプ 01 の 8 指標はどれも計測が無い**
（稼働率 30 日 / API 応答 p95 / 同期キュー / 同期の失敗 / 写真ストレージ /
表の p95・直近 24 時間・エラー率）。加えて事象履歴の表も、24 時間の
リクエスト量も、オフライン同期の 4 行も出す元が無い。
→ **OPEN_QUESTIONS #114**（3 案と推奨を書いた。1 案は課金が発生するので
オーナーの判断が要る）。

**出す元が在るのはコンポーネントの状態だけ。** `checkHealth()` が
shards / storage / cache / queues を `ok` / `degraded` で返す。
これだけで画面を組んだ:

- 全体の状態（1 行）
- 表「コンポーネントの状態」= **コンポーネント列と状態列の 2 列だけ。**
  応答 p95・直近 24 時間・エラー率は**列ごと置いていない。**
  データベースの行にだけ「到達本数 / 期待本数」を添える
  （**番号ではなく件数** — `ShardHealth` は型の上で番号を持たない）。
- **計測が無いことを画面に書いた**（「仮の数字は置きません」）。
  空欄で察させない。
- 逐語の注記 2 つ。

**メンテナンス時間帯の注記が PF-14 の既定と 1 時間ずれていた**
（注記 02:00〜04:00 / PF-14 の表 03:00〜04:00）→ OPEN_QUESTIONS #115。
**2026-08-20 のオーナー判断で 03:00〜04:00 に統一**（DECISIONS #239）。
注記のほうを直し、境界（03:00 以上 04:00 未満）も文に入れた。
設定の既定は動かしていない。

`routes/plat/status.spec.ts`（9 件）が固定したもの: KPI の語が文言に無い、
プロトタイプの数字がソースに無い、`?? 0` で無い値を 0 に落としていない、
シャードの binding 名に触れていない、`action` と `<Form>` が無い、
注記 2 つが逐語であること。

## 決着（2026-08-20 / オーナー判断）
**OPEN_QUESTIONS #114 は案 2 に決まった**（DECISIONS #238）。
収集は **PF-15** が作り、この画面が読む。

- 出すのは **稼働率 / 事象件数 / 事象履歴 / 最終更新日時** の 4 つ
- **p95 とエラー率は作らない**（集計だけでは作れない）
- 計測できない指標は **0 も仮値も出さず「未計測」と明示する**
- **Analytics Engine の追加契約は行わない**

**この task は PF-15 の完了後に閉じる。**
