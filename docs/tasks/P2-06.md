# P2-06 M-09 検査実施

**Phase**: 2
**仕様**: docs/PK-SPEC-P2.md §11.3
**依存**: P2-05
**見積**: 1.5日
**ステータス**: 完了（2026-08-13）

## 読むべきルール
ui-writing.md §3

## やること
項目別の合否入力。

## 完了条件
- [x] **初期値が全 PASS になっていない**（未選択から始まる）
      画面は `item.status`（`null` = 未選択）をそのまま出す。
      サーバー側も行を先に作らない（「行が無い＝まだ見ていない」/ P2-04）。
- [x] **「全て合格」ボタンがない**
      ボタンも、ループして全項目を送るコードも無い。API も 1 項目ずつしか
      受けない（DECISIONS #064）。**画面だけで守っていない。**

## 作業ログ

### 2026-08-13

- 画面 `/m/inspection/:inspectionId`。項目別の 3 値（合格/不合格/対象外）。
- 不合格を選んだときだけ理由コード・気づいたこと・写真・再清掃の要否を出す。
- 写真は `input[type=file][capture=environment]`（**`getUserMedia()` を
  使わない** / security.md §4）。クライアント側で `preparePhoto()` が
  1600px・JPEG q=0.7 へ縮め、サーバー側が改めて EXIF を落とす。
- 完了は 1 ボタン。足りないものは 409 の `details` を受けて項目に印を付ける。

**止まらずに判断したこと**
- OPEN_QUESTIONS #047: **M-09 の画面設計が仕様書とプロトタイプで食い違う。**
  `ui-prototypes/mobile/pk-13-inspection.html` は部屋単位の二択で、
  「24 時間後に自動で合格」まで書いている。**仕様書 §11.3 を採った**
  （項目別の 3 値）。理由は task ファイルの完了条件そのものが項目別入力を
  前提にしていること、および §2.3 が「検査なしを検査合格として集計しない」と
  定めていること。
