# P4-14 月次監査レポート PDF

**Phase**: 4
**仕様**: docs/PK-SPEC-P4.md §7
**依存**: P4-12
**見積**: 2日
**ステータス**: 完了（2026-08-14）

## 読むべきルール
architecture.md §5

## やること
オーナー向け月次レポート。

## 完了条件
- [x] 6 セクションが出力される
- [x] **免責文が全文固定で、編集できない**
- [x] Queue コンシューマ内で生成される

## 作業ログ

### 2026-08-14
- payload は `packages/engine/src/reconciliation/auditReport.ts`（純粋関数）、
  テンプレートは `packages/pdf/src/auditReport.ts`、生成は
  `apps/web/src/consumers/auditReport.ts`。
- **免責文（§7.2 MUST）は engine の定数。** payload に入れてはいるが、
  **テンプレートは定数を直に読む。** payload を書き換えても出力は変わらない
  （`auditReport.spec.ts` が両方を固定）。`buildAuditReportPayload()` は
  文言を引数に取らない。
- 6 セクションは該当が 0 件でも見出しごと残し、「該当なし」を出す。
- `pdf-generation` キューに 2 種類（日報・監査レポート）が載る形にした。
  種別ごとにキューを増やさない（wrangler の無料枠）。
- **表に行を作らない**（DECISIONS #119）。R2 のキーが
  `(組織, 施設, 月)` で決まるので、3 回作っても同じキーに同じ内容が載る。
- 12 か月の推移は `countFindingsByMonth()` で 1 回だけ読む
  （月ごとに 12 クエリにしない）。業務日の先頭 7 文字で GROUP BY する。

## 止まらずに判断したこと
- 保存先の表を作らなかった（DECISIONS #119）。§7 が表を定めておらず、
  このレポートは元データからいつでも作り直せる要約で、
  発行済み帳票（billing.md §2）ではない。
