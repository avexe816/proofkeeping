# P1-17 M-11 自分の実績

**Phase**: 1
**仕様**: docs/PK-SPEC-P1.md §9.6
**依存**: P1-08
**見積**: 0.5日
**ステータス**: 完了

## 読むべきルール
security.md §5

## やること
自分の完了件数・作業時間のみ表示。

## 完了条件
- [x] **他人との比較・ランキングを表示しない**

## 作業ログ

### 2026-08-12 実装

- `packages/engine/src/ownWork.ts` … 集計と週の範囲（純粋関数）
- `apps/web/src/routes/m/me.tsx` … `/m/me`
- `apps/web/src/lib/auth/permission.ts` … `task.readOwn` を追加

**他人が入り込む余地を型で塞いだ。** `summarizeOwnWork()` は 1 人ぶんの
タスクしか受け取らず、比較対象を引数に持たない。担当者 ID を受け取る口も
無い（loader がセッションの `membershipId` を使う）。

**平均だけを 20 件の閾値で伏せる**（security.md §5 / DECISIONS #035）。
件数と合計作業時間は事実なので常に出す。仕様書・プロトタイプとの
食い違いは OPEN_QUESTIONS #040。

`PermissionScope` に `SELF` を足していない。理由は
`lib/auth/permission.ts` の `task.readOwn` の注記。

