# P5-02 取引先マスタ

**Phase**: 5
**仕様**: docs/PK-SPEC-P5.md §2.1
**依存**: P5-01
**見積**: 1日
**ステータス**: 完了

## 読むべきルール
（CLAUDE.md のみ）

## やること
取引先の登録・編集。

## 完了条件
- [x] 締め日・支払サイト・端数処理を設定できる
- [x] 請求先メールと CC を登録できる

## 作業ログ
- 2026-08-14: 実装。
  - `packages/contracts/src/billing.ts` — 取引先の Zod スキーマ。
    登録番号の空文字は `null` へ落とす（「設定済みだが空」を作らない）。
    CC は 10 件まで（送付ログにそのまま載るため）。
  - `packages/db/src/repositories/invoice.ts` — `updateCounterparty()` を追加。
    **`code` を受けない**（鍵の付け替えは新しい取引先を作る操作）。
  - `apps/web/src/routes/api/v1/counterparties.ts` — GET / POST / PATCH。
    **`DELETE` は無い**（CLAUDE.md §4）。無効化は `PATCH { isActive: false }`。
  - 権限 `billing.write` を新設（`OWNER` / `ORG_ADMIN` のみ）。単価と請求先は
    施設の運用者が触れる範囲を超える（security.md §1 に根拠が無い）。
  - 監査 `counterparty.created` / `counterparty.updated` を追加。
    **住所と担当者名を `before` / `after` に載せない**（必要のない個人の情報を
    消せない表に積まない）。
