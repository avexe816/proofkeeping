# P8-11 在留資格の保存期間と削除

**Phase**: 8（起票 2026-08-22 / **オーナー指示** / DECISIONS #261）
**仕様**: `docs/PK-SPEC-P8.md` §1.4 / `.claude/rules/security.md` §4・§5
**依存**: P8-02（在留資格の記録）
**見積**: 1日
**優先度**: **production の開通、または実在する従業員データの投入前に完了必須**
**ステータス**: 完了（2026-08-22 / DECISIONS #266）

## 背景

**在留資格の情報に保存期間の定めが無い。** 写真は 6 か月・上位プランで
13 か月（security.md §4）、退避は 13 か月（PK-SPEC-P0 §19.7）と定めがあるのに、
`residency_record` には期間の定めも削除の仕組みも無い。

**現在は無期限保持で、削除の経路が 1 本も無い。** `residency` は
archive / retention のどのコードにも現れない（2026-08-22 時点で確認）。
退職者の在留資格が、退職後もそのまま残り続ける。

INV-08 v2（DECISIONS #261）で閲覧できる相手を `OWNER` へ広げたので、
**残り続ける情報を読める人も増えた。** 期間を決めるのはその見返りにあたる。

## 法令の状況（起票時点の整理。**実装前に最新を確かめること**）

- **労働者名簿・賃金台帳等の法定保存期間は、退職日起算で 5 年。**
  ただし**経過措置により当分の間は 3 年**（労働基準法 109 条・143 条）。
- **在留カードの写しそのものには、明確な年数の定めが無い。**
  実務は「在留期間中は保管し、退職後も一定期間は保管」という運用が多い。
- **退職時に在留カード番号の記録を削除する実務もある**（保持の必要が
  無くなった個人情報を残さない、という考え方）。
  なお **ProofKeeping は在留カード番号を保存していない**（`residency_record` に
  その列が無い）。ここで扱うのは種別・期限・更新申請日だけ。

## 保存・削除の方針（**オーナー判断で確定 / 2026-08-22**）

> **在留資格の記録は、従業員の退職日から 3 年が経過した時点で物理削除する。**

確定した仕様。**実装はこの定めに従う。**

### 起算と判定

- **起算日は従業員の退職日。** 在留期限でも、記録を作った日でもない。
- **退職から 3 年未満の記録は削除しない。**
- **退職から 3 年が経過した在留資格の記録は物理削除する。**
- **退職日が不明な場合は自動削除しない。** 判定の材料が無い記録を、
  経過日数の推測で消さない。人が退職日を入れてから対象になる。
- **在職中の記録は、在留期限が切れていても削除しない。** 期限切れは
  削除の理由にならない（配分停止の理由にはなる / `assignmentBlock.ts`）。

### 通知との関係

- **退職した時点で、期限通知の対象から即座に外す。** 保存期間とは別に、
  すぐ効かせる。退職者へ更新の通知が飛ぶ状態を作らない。
  （現在の `countResidencyAlerts()` は `workStatus = RESIGNED` を数えないので、
  **ここは既に満たしている。** 削除の実装で壊さないこと。）

### 削除の対象と方法

- **削除の対象は在留資格の記録**（`residency_record` の行）。
- **在留カード番号は対象外。** ProofKeeping は在留カード番号を保存して
  いない（`residency_record` にその列が無い）。**列を足さないこと。**
- **物理削除にする。** 訂正の履歴を残す `EvidenceSnapshot` や帳票と違い、
  在留資格は「持たないこと」に意味がある。
- **削除処理は冪等。** 複数回実行しても結果が変わらない（testing.md §4）。

### 監査ログ

- 削除の実行時に **`residency.deleted`** を監査ログへ記録する
  （`AUDIT_ACTIONS` へ 1 行足す）。
- **監査ログに氏名・在留資格の種別・期限・更新申請日などの値を残さない。**
  `residency.viewed`（DECISIONS #261）と同じ扱いで、記録の口を
  **値を受け取れない形**にすること。
- **削除済みの在留資格の情報を監査ログから復元できる形にしない。**
  監査ログが「消したはずの情報」の控えになってはならない。

### 実装前に別途確かめること

- **バックアップや退避データに残る期間と、そこからの削除方法。**
  D1 のバックアップ、`ARCHIVE` の退避（PK-SPEC-P0 §19.7）に在留資格が
  含まれるかを確かめ、含まれるなら消し方を決めてから実装する。

## 完了条件

- [x] 保存期間（**退職日から 3 年**）が仕様（PK-SPEC-P8 §1.4）に書かれている
      → v1.2 で追記
- [x] 退職後の経過日数で削除対象を選ぶ純粋関数がある（正例・負例 5 件ずつ）
      → `apps/web/src/lib/staff/residencyRetention.ts` / spec 26 件
  - [x] **退職日が不明なら対象にしない**
  - [x] **在職中は在留期限が切れていても対象にしない**
  - [x] 3 年ちょうど・3 年未満の境目を固定している
- [x] 削除のバッチがあり、**3 回実行しても結果が変わらない**（testing.md §4）
      → `apps/web/src/consumers/residencyRetention.ts`。**cron もキューも増やして
      いない**（期限アラートの日次バッチに相乗り）
- [x] 削除を監査ログに残す（`residency.deleted` を `AUDIT_ACTIONS` へ）
- [x] **監査ログに値が入らないことが検査で固定されている**（氏名・種別・
      期限・更新申請日。`residency.viewed` と同じ形）
      → 口は `recordResidencyDeletion()` の 1 つで、**引数は件数だけ**
- [x] **削除済みの情報を監査ログから復元できないことが検査で固定されている**
      → スタッフ ID も在留資格の行 ID も載らないことを spec が固定
- [x] **退職者へ更新の通知が飛ばないことが検査で固定されている**
      → `residencyAlert.spec.ts`（`countResidencyAlerts()` は `RESIGNED` を数えない）
- [x] 利用目的（security.md §5）と保存期間が食い違っていない
      → §5 の利用目的は「在留資格および在留期限の管理」。保存期間の定めは
      その目的が終わったあとに残さないことを担保する
- [x] バックアップ・退避データに残る期間と削除方法を確かめてある
      → **`residency_record` は退避の対象ではない**（`packages/db/src/archivePolicy.ts` の
      `DIRECTLY_ARCHIVABLE_TABLES` は `cleaning_task` / `room_observation` /
      `linen_record` / `occupancy_snapshot` / `physical_signal` の 5 つのみ）。
      R2 に写しが無いので、D1 の行を消せば消える。**D1 のバックアップ（Time Travel）
      は Cloudflare 側の保持期間に従い、こちらから消せない** — production を
      開通させるときに保持期間を確認すること（下の申し送り）

## やらないこと

- **在留カード番号の保存**（列を作らない。INV-08 v2 の趣旨に反する）
- 削除の取り消し（復元の経路を作らない）
- 退職の判定そのものの変更（`workStatus` の扱いは P8-01 のまま）
- **保存期間を設定で変えられるようにすること**（PK-IMPL-CONTRACT §11.4 の
  「設定可能にすると運用中の判断でいずれ変更される」と同じ理由。
  変更にはリリースを要する状態を保つ）

## 実装（2026-08-22）

| 追加・変更 | 場所 |
|---|---|
| 削除対象を選ぶ純粋関数 | `apps/web/src/lib/staff/residencyRetention.ts` |
| 削除のバッチ | `apps/web/src/consumers/residencyRetention.ts` |
| 日次バッチへの相乗り | `apps/web/src/consumers/residencyAlert.ts` |
| 物理削除のリポジトリ関数 | `packages/db/src/repositories/residency.ts`（`deleteResidencyRecords`） |
| 監査ログの口（**件数しか受け取れない**） | `apps/web/src/lib/staff/residencyRetentionAudit.ts`（`recordResidencyDeletion`） |
| 監査対象の追加 | `packages/db/src/repositories/audit.ts`（`residency.deleted`） |
| 仕様 | `docs/PK-SPEC-P8.md` §1.4（v1.2） |

**migration は無い。** 列も表も増えていない（消すだけ）。

## 申し送り

- **D1 のバックアップ（Time Travel）に残る期間は Cloudflare 側の定めで、
  アプリケーションから消せない。** production を開通させる前に、保持期間と、
  それが「退職日から 3 年」の定めと矛盾しないかを確認すること。
  矛盾するなら、保持期間の短縮か、利用者への説明のどちらかが要る。
