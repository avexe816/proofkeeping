# P1-18 多言語（英語）

**Phase**: 1
**仕様**: docs/PK-SPEC-P1.md §12
**依存**: P1-10
**見積**: 1日
**ステータス**: 完了

## 読むべきルール
ui-writing.md §1

## やること
モバイル画面の英語対応。

## 完了条件
- [x] モバイル全画面が英語で表示される
- [x] チェックリスト項目が多言語 JSON で持てる
- [x] 未翻訳時は日本語＋「日本語のみ」表示

## 作業ログ

### 2026-08-12 実装

- `apps/web/src/locales/en.json` … モバイル全画面（`m.*`）と客室ボードの英語
- `apps/web/src/routes/m/me.tsx` … 言語切替（§12.3「M-11 の設定画面から」）
- `packages/db/src/repositories/user.ts` … `setUserLocale()`
- `apps/web/src/routes/m/checklist.tsx` … 未翻訳に「日本語のみ」

チェックリストの多言語は既に `checklistItem.labels`（JSON）が持っている
（P1-06）。**未翻訳なら日本語を出し、項目名の横に「日本語のみ」**（§12.2）。

**ブラウザの言語設定を参照しない**（ui-writing.md §1）。ユーザー属性
（`user.locale`）→ 組織の既定 → `ja` の 3 段だけ。

管理画面は日本語のみ（§12.1）。PC 専用のキーは意図的に訳していない
（欠けたキーは `ja` に落ちる）。

