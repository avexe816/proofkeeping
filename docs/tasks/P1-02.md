# P1-02 標準時間マスタと設定画面

**Phase**: 1
**仕様**: docs/PK-SPEC-P1.md §3.1, §10.1
**依存**: P1-01
**見積**: 0.5日
**ステータス**: 完了

## 読むべきルール
（CLAUDE.md のみ）

## やること
W-17 標準時間設定画面。

## 完了条件
- [ ] 客室タイプ×清掃種別で標準時間を設定できる

## 作業ログ

### 2026-08-12 — 完了（API のみ）

**やったこと**

- `standardTime` 表（P1-01 で作成）に対するリポジトリ
  （`packages/db/src/repositories/standardTime.ts`）。
- `GET / PUT /api/v1/standard-times`（`apps/web/src/routes/api/v1/standardTimes.ts`）。
- 権限 `standardTime.read` / `standardTime.write` を追加。§10.1 が W-17 を
  `ORG_ADMIN` の画面と定めるため、**施設責任者に広げていない**（§11.5）。
- seed が施設 × 客室タイプ × {CHECKOUT, STAYOVER} を投入する。
  値は §3.1 の既定分数と同じ（違う値だと生成結果の説明がつかない）。

**完了条件**

- [x] 客室タイプ×清掃種別で標準時間を設定できる
      → `PUT /api/v1/standard-times`。upsert なので 3 回送っても行が増えない。

**未達（意図的）**

- **W-17 の画面は作っていない。** この batch はバックエンドのみ。
  task 名の「設定画面」は Batch 2/3 で作る。
- 設定を消す API を作っていない。画面が一部だけ送ったときに、
  送られなかった行が黙って消えるのを避けるため（必要になったら明示的な口を足す）。

