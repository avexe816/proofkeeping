# P6-01 スキーマ: 連携

**Phase**: 6
**仕様**: docs/PK-SPEC-P6.md §2
**依存**: P5 完了
**見積**: 1日
**ステータス**: 完了

## 読むべきルール
security.md §7

## やること
`integration`, `syncLog`, `externalMapping`, `pushSubscription`,
`notificationPreference`, `apiKey`, `outboundWebhook`

## 完了条件
- [x] 認証情報のカラムに平文を入れない設計になっている
- [x] 越境テストが全テーブルにある

## 作業ログ

2026-08-14 完了。

**7 表を足した。** `integration` / `sync_log` / `push_subscription` /
`notification_preference` / `api_key` / `outbound_webhook` と、P0-22 が定義だけ
置いていた `external_mapping`（列は P6 の語彙に揃えた）。
migration は `0017_p6_01_integration.sql`。

**平文の資格情報を持つ列が 1 つも無い**（DECISIONS #138）。仕様 §2.1 の
`webhookSecret` と §6.4 の `secret` は平文の列だが、security.md §7 と
§8.5 の受け入れ基準（「DB に平文がない」）に従って
`webhookSecretRef` / `secretRef`（KV の参照キー）に読み替えた。
`apiKey.keyHash` はハッシュなので列のまま。

`pushSubscription.p256dh` / `auth` は**あえて KV へ出していない。**
端末が発行した宛先の一部で、漏れて起きるのは「その端末へ通知を送れる」こと。
外部システムへのログイン情報と危険度が違う。

**越境テスト**は `integration` / `sync_log` / `external_mapping` の 3 表を
`tests/tenant-isolation/integration.spec.ts` でカバーした。残る 4 表
（通知・公開 API）は読み書きする関数がまだ無いので `UNCOVERED_TABLES` に
理由付きで載せ、P6-09 / P6-10 / P6-12 / P6-13 が消す。

### 止まらずに判断したこと
- DECISIONS #138（秘密は KV の参照キーだけを持つ）
- DECISIONS #139（7 表ぶんの entityPrefix）
- OPEN_QUESTIONS #085（組織全体の連携が重複して作れる）
- OPEN_QUESTIONS #086（Bearer トークンから組織を解決する手段が無い）
