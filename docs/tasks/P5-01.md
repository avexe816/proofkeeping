# P5-01 スキーマ: 請求・領収

**Phase**: 5
**仕様**: docs/PK-SPEC-P5.md §2
**依存**: P4 完了
**見積**: 1.5日
**ステータス**: 完了（2026-08-14）

## 読むべきルール
billing.md §2

## やること
`counterparty`, `pricingRule`, `invoice`, `invoiceLine`,
`invoiceTaxSummary`, `receipt`, `documentDelivery`, `billingPeriod`

## 完了条件
- [x] `issueDate` / `totalAmount` / `counterpartyName` が非正規化されている
- [x] 3 項目の複合インデックスがある（電帳法の検索要件）
- [x] 越境テストが全テーブルにある

## 作業ログ

### 2026-08-14
- 8 表を `packages/db/src/schema/invoice.ts` に置いた。
  **既存の `billing.ts` とは別ファイル。** あちらは自社が顧客から受け取る
  契約（subscription / entitlement）で、こちらは顧客が取引先へ出す
  請求書と領収書。金額の意味も、消せるかどうかも違う。
- 電帳法の検索 3 項目（§1.2 MUST）は請求書と領収書の**両方**に
  非正規化し、`idx_inv_search`（発行日 × 金額）と
  `idx_inv_party`（取引先名）を張った。**後から足すと再構築が要る**
  （billing.md §2）。
- 仕様に無い `organizationId` を `invoiceLine` / `invoiceTaxSummary` に
  足した。親の `invoice` を辿らないと組織が分からない形にすると、
  リポジトリ層の強制注入（architecture.md §2 第 1 層）を掛けられない。
- 読み取りリポジトリ 16 関数と、マスタの書き込み 2 つ
  （`upsertCounterparty()` / `insertPricingRule()`）まで。
  **発行・訂正・送付は P5-07 以降**（§4.1 の 10 手順を分割しない）。
- 越境テストは 8 表ぶん（`tests/tenant-isolation/invoice.spec.ts`）。
  **第 4 パターン（施設スコープ）は成立しない**ので
  `propertyColumn: null` を明示した（DECISIONS #121）。
- `repositories.spec.ts` に「発行済み帳票」の検査を足した。
  `invoice` / `receipt` / `invoiceLine` / `invoiceTaxSummary` /
  `documentDelivery` を DELETE する関数が無いこと、
  **請求書の金額を引数に取る更新関数が無いこと**をソース走査で固定する。

## 止まらずに判断したこと
- DECISIONS #120 6 表の entityPrefix。
- DECISIONS #121 施設スコープを掛けず `NO_PROPERTY_SCOPE` を明示する。
