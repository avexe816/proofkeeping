# P1-16 客室ステータス同期

**Phase**: 1
**仕様**: docs/PK-SPEC-P1.md §11
**依存**: P1-05
**見積**: 1日
**ステータス**: 完了

## 読むべきルール
security.md §6

## やること
タスク状態と客室状態の連動。

## 完了条件
- [x] タスク状態と客室状態が連動する
- [x] 手動上書きに理由が必須
- [x] **手動上書きが `AuditLog` に記録される**（P4 の R010 で使う）

## 作業ログ

### 2026-08-12 実装

- `packages/db/src/schema/property.ts` … `room.housekeepingStatus`（既定 `DIRTY`）
- `packages/db/migrations/0004_p1_16_room_status.sql` … 列の追加（後方互換）
- `packages/engine/src/roomStatus.ts` … §11.1 の対応表（純粋関数）
- `apps/web/src/lib/room/status.ts` … 手動上書き（理由必須 + 監査）
- `apps/web/src/lib/task/transition.ts` / `generate.ts` … 同期の呼び出し

OPEN_QUESTIONS #034 を解消した。

**`READY` になるのは検査が終わってから**（§11.1 MUST）。検査必要の施設は
`complete` で `INSPECTING` に止まる。

手動上書きは 3 つとも必須: 権限（施設は客室から解決 / INV-32）・理由の
必須・`AuditLog` の `room.statusOverridden`。**P4 の R010 が使う。**

自動同期は監査ログに残さない（元の操作が既に残っている）。

`unblock` の行が §11.1 に無い。`DIRTY` へ戻す判断をした
（OPEN_QUESTIONS #038）。

