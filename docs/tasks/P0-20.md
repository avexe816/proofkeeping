# P0-20 ヘルスチェックと監視

**Phase**: 0
**仕様**: docs/PK-SPEC-P0.md §13.8
**依存**: P0-06
**見積**: 0.5日
**ステータス**: 完了（範囲を /api/health に限定）

## 読むべきルール
（CLAUDE.md のみ）

## やること
`/api/health` と Sentry 連携。

## 完了条件
- [x] `/api/health` が 16 シャード・R2・KV・Queue の状態を返す（**件数と真偽のみ。番号を出さない**）
- [ ] `schema_version` 不一致で書き込み系 API が 503 になる
- [ ] Sentry にエラーが集約される

## 作業ログ

### 2026-08-12
- **`GET /api/health` の 1 経路のみ。** 認証を要求しない。
- 返すのは**件数と真偽だけ。** §13.8 は「16 シャードの状態」と読めるが、
  architecture.md §1 がシャード番号の露出を禁じており、しかもこの経路は
  認証を要求しない。どの番号が落ちているかは含めない。応答の JSON を
  走査して `SHARD_\d` と例外メッセージが混ざらないことを spec で押さえた。
- `degraded` は 503。外形監視が本文を解釈せずに落ちを検出できる。
- Queue は binding の宣言までを見る。**確認のためにメッセージを送らない**
  （毎回の疎通で消費者へ空メッセージが流れる）。
- `env.SHARD_XX` を列挙してよいのは `router.ts` だけなので
  `listShardDatabases()` をそこへ置いた。返り値に番号を含めない。
- **未達 2 件:**
  - `schema_version` 不一致で書き込み系 API を 503 にする middleware（§19.8）。
    毎リクエストで 16 シャードを引けないためキャッシュ設計が要る。
  - Sentry 連携。**採用可否が PK-SPEC-P0 §20 で未決**（OPEN_QUESTIONS #030）。
