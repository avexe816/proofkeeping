# P7-15 顧客向けドキュメント

**Phase**: 7
**仕様**: docs/PK-SPEC-P7.md §7.1
**依存**: P7-13
**見積**: 3日
**ステータス**: 完了（2026-08-15）

## 読むべきルール
（CLAUDE.md のみ）

## やること
7 文書の作成。

## 完了条件
- [x] 7 文書が公開されている
- [x] **清掃スタッフ向けガイドが A4 1 枚**（日英）
- [x] 「差異レポートの読み方」が用意され、Audit 有効化時に同意を求める

## 作業ログ

### 2026-08-15

`docs/guides/` に 6 文書を置き、API リファレンス（P6-15 の
`docs/PK-API.md`）と合わせて §7.1 の 7 文書とした。

| 文書 | 実体 |
|---|---|
| 索引 | `docs/guides/README.md` |
| はじめかたガイド | `getting-started.md` |
| 清掃スタッフ向け操作ガイド | `cleaner-guide.html`（**A4 × 日英 2 枚**） |
| 検査担当者向けガイド | `inspector-guide.html`（**A4 1 枚**） |
| 管理者マニュアル | `admin-manual.md` |
| 差異レポートの読み方 | `finding-report-reading.md` |
| よくある質問 | `faq.md`（**30 項目**） |
| 公開 API リファレンス | `../PK-API.md`（既存） |

#### A4 1 枚（§7.1 MUST）

`.sheet` を 210mm × 297mm に固定し、`@page` の余白を 0 にした。
Chromium の印刷で **清掃スタッフ向け = 2 ページ（日 1・英 1）**、
**検査担当者向け = 1 ページ**になることを実測で確かめた。

`tests/docs/customerDocs.spec.ts` が、寸法・手順数（8）・1 枚あたりの
本文の分量（1,400 字）の 3 つで押さえる。**寸法だけだと項目を足したときに
素通りする。**

#### Audit 有効化時の同意（§7.3 MUST）

`module_entitlement` への書き込みは 1 本も無い（P7-04 が担当。未着手）。
そこで **有効化の前提として通す門**を先に置いた。

- `apps/web/src/lib/moduleConsent.ts` — `assertModuleConsent()`。
  文書のパスと**版数**を照合する。**無効化は止めない。**
- `moduleConsent.spec.ts` — 判定 / 文書の版数の一致 / §7.3 の 7 要点 /
  **書き込みが生えたら門を通っているかの走査**。

判定の順序は `assertPermission`（404）→ `assertEntitlement`（402）→
`assertModuleConsent`（409）。DECISIONS #173。
同意を**どこに保存するか**は決めていない（OPEN_QUESTIONS #099）。
HTTP 409 への写像も P7-04 が経路と一緒に足す。

#### 止まらずに判断したこと

- DECISIONS #173 同意は「有効化という操作」の条件として置く
- DECISIONS #174 禁止語の規則を顧客向け文書にも当てる（否定形でも使わない）
- OPEN_QUESTIONS #099 同意の記録先が仕様に無い
- OPEN_QUESTIONS #100 申込み〜組織作成の経路が無い（P7-01 待ち）
