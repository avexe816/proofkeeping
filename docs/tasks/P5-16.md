# P5-16 発注元閲覧ロール `CLIENT_VIEWER`

**Phase**: P5 の拡張（追加起票 / 2026-08-19 / オーナー指示）
**仕様**: docs/PK-IMPL-CONTRACT.md §4（OWNER / VIEWER 列が権限の正） / docs/PK-SPEC-P5.md §6（双方合意フロー） / docs/PK-BIZ-PLAN.md §3.4・§9-5
**依存**: P5-12（合意履歴）/ P7-18〜20（メンバー管理・権限）
**見積**: 2日
**ステータス**: 実装済み（2026-08-19）

## この task が起票された理由

事業目的「ホテル運営側にもアカウントを発行し、清掃結果を確認・照合できるようにする」
に対して、現行の 7 ロールに**発注元（ホテル側）を正しく写像できるロールが無い**。

| 候補 | 何が壊れるか |
|---|---|
| `OWNER` を発行 | 組織全体の最強権限（組織設定・税務・照合実行の write）。発注元が受注者の設定を書き換えられる |
| `AUDITOR` を発行 | 組織全体が読める。「自施設のみ」（契約 §4）に絞れない |

これは OPEN_QUESTIONS **#011**（契約 §4 の `OWNER`/`VIEWER` と security.md §1 の
`OWNER` が別概念）の未決部分そのものであり、この task で決着させる。

## 読むべきルール
- `.claude/rules/security.md` §1（ロールと境界）
- `.claude/rules/architecture.md` §2（テナント分離）
- `.claude/rules/testing.md` §2（越境テスト）
- `docs/PK-IMPL-CONTRACT.md` §4（ビジネス権限表。この task の権限の正）

## やること

### 1. ロール定義
- `packages/db/src/schema/user.ts` の `ROLES` に `CLIENT_VIEWER` を追加（8 語目）。
  表示名は「発注元」。DB は素の text 列のためスキーマ変更不要。
- `membership` に `counterpartyId`（nullable）を追加。CLIENT_VIEWER のみ設定。
  additive migration（`pnpm db:generate`）。

### 2. 権限
- `PERMISSION_MATRIX` に CLIENT_VIEWER の全セルを記入。**フォールバック禁止**。
  権限の正は契約 §4 の OWNER/VIEWER 列: 清掃記録・検査結果・差異・日報・月次
  レポートは **ASSIGNED（担当施設）で read**、write は原則すべて DENY、
  氏名 ×・個人別実績 ×・シフト ×・検査実施 ×・手順変更 ×・請求確定 ×。
- 新アクション `billing.review`（合意/差戻し専用・write）を追加。
  CLIENT_VIEWER は自 counterparty の請求期間のみ `byCounterparty: true` で実行可。
- `ORG_WIDE_ROLES` には**載せない**（`allowedPropertyIds` 絞りを既定で効かせる）。
- `STAFF_NAME_HIDDEN_ROLES` に追加（契約 §4「清掃員の氏名 ×」）。

### 3. counterparty スコープ（第 1 層防御の拡張）
- `TenantContext` に `counterpartyId`（nullable）を追加。`middleware/tenant.ts` が
  membership から組み立てる。
- billingPeriod / invoice / receipt 系リポジトリで
  `ctx.counterpartyId != null` のとき counterpartyId の一致を**強制注入**する
  （`organizationId` 強制注入と同じ向き）。

### 4. メンバー管理・認証
- `PASSWORD_LOGIN_ROLES` / `ADMIN_STAFF_ROLES` / `ADMIN_ROLE_OPTIONS` /
  `ASSIGNED_ROLES` に追加。members 画面で CLIENT_VIEWER 選択時は
  **取引先の選択と施設割当を必須**にする。
- `ROLE_LABEL`（members.tsx / UserMenu.tsx）と `ja.json` / `en.json` に表示名。

## 完了条件
- [ ] CLIENT_VIEWER でログインし、担当施設の清掃記録・検査・差異・日報を閲覧できる
- [ ] 清掃スタッフの氏名がすべての画面で「非表示」になる
- [ ] 担当外施設・他 counterparty の請求期間は 404（403 を返さない）
- [ ] write 系 API がすべて 404
- [ ] 自 counterparty の請求期間を承認・差戻しできる（`byCounterparty: true`）
- [ ] `pnpm check` 通過（permission.spec の EXPECTED 8 桁化・listScope 3 表・
      schema.spec の ROLES・navigation.spec を更新）

## やらないこと
- **別テナント（ホテル法人の独立組織）との相互参照。** `user` は組織スコープで
  シャード分散しており、組織間参照は全シャード走査禁止（architecture.md §3）に抵触する。
- **契約 §4 の「再清掃の依頼（依頼可）」。** 依頼→運営管理者経由の経路（契約 §4.2）は
  通知基盤の整理後に別 task で起票する。
- **閲覧者の招待（契約 §4「自施設」）。** user.write は DENY のまま。招待は
  ORG_ADMIN 以上が行う。

## 作業ログ

2026-08-19 実装完了（バッチ: レビュー指示 1〜2）。

### やったこと

| 区分 | 内容 |
|---|---|
| ロール | `ROLES` に `CLIENT_VIEWER`（8 語目）。`membership.counterpartyId`（migration 0024） |
| 権限 | `PERMISSION_MATRIX` に全セル記入。read 8 個（organization/property/finding/task/roomPlan/inspection/dailyReport/billing）＋ `billing.review` のみ許可 |
| 権限 | `billing.readInternal`（契約と請求・清掃会社プラン・取引先設定・送付ログ）と `auditLog.read` を `billing.read` / `finding.read` から**分離**。発注元に収支・自社契約・操作履歴・他取引先の宛先が漏れる経路を塞いだ |
| 第1層 | `scopeToCounterparty()`（base.ts）。請求系リポジトリ 10 関数に強制注入。未設定の CLIENT_VIEWER は 0 件 |
| 画面 | members から取引先必須で登録。ROLE_FAMILY 拡張（CLIENT_VIEWER への/からのロール変更禁止） |
| テスト | permission.spec EXPECTED 8 桁化・不変条件 3 本追加 / base.spec に counterparty 絞り 4 本 / navigation.spec に可視 8 項目の固定 |

### 判断したこと（DECISIONS に記録済み）

- #205 CLIENT_VIEWER の設計（1 組織内ロール・counterparty スコープ・氏名非表示）
- #206 billing.readInternal / auditLog.read の分離

### 申し送り

- **user.read は DENY。** ユーザー一覧 API は氏名をマスクせずに返すため、発注元には一覧ごと閉じた。
- **請求の PC 画面は発注元向けにまだ無い。** 承認はメールリンク（P5-17）か API 経由。発注元向け請求確認画面は次の起票。
- 契約 §4 の「再清掃の依頼」「閲覧者の招待」は未実装（task 本文の「やらないこと」どおり）。
