# P4-09 抑制ロジック

**Phase**: 4
**仕様**: docs/PK-SPEC-P4.md §4
**依存**: P4-08
**見積**: 1.5日
**ステータス**: 完了（2026-08-14）

## 読むべきルール
（CLAUDE.md のみ）

## やること
自動抑制と重要度引き下げ。

## 完了条件
- [x] 自動抑制 6 条件が動く
- [x] 重要度の自動引き下げ 4 条件が動く
- [x] 抑制件数が Run に記録され、可視化される

## 作業ログ

### 2026-08-14
**実体は P4-03 と P4-06 で先に入っていた。** この task では欠けていた
配線を埋め、条件が実際に届くことを確かめた。

- §4.1 の 6 条件は `packages/engine/src/reconciliation/suppression.ts`
  （`ROOM_NOT_ON_SALE` / `ACCESS_LOG_REGISTERED` /
  `HOUSE_USE_OR_COMPLIMENTARY` / `OCCUPANCY_NOT_LINKED` /
  `OPERATION_TOO_NEW` / `RULE_DISABLED`）。仕様に無い
  `SOURCE_UNAVAILABLE` を 7 つめとして持っている（§1.2 の系統欠落を
  「該当なし」と区別するため）。
- §4.2 の 4 条件は `confidence.ts`。ただし**「ベースラインの
  `sampleSize` が 20〜40 なら −10」だけが届いていなかった。**
  照合が `baselines: []` を渡していたため（P4-05 の暫定）。
  P4-11 でベースラインを渡すようにして、この条件が初めて効くようになった。
- §4.3 の可視化は P4-06（W-06 の「抑制された差異 N 件」）。
  件数は `reconciliationRun.findingsSuppressed` の合計から出す。

## 止まらずに判断したこと
- 抑制の**理由**（内訳）は API にも画面にも出していない。§4.3 が求めるのは
  件数の可視化で、理由ごとの内訳は仕様に無い。engine は
  `SuppressedRule[]` として理由を返しているので、要るときに拾える。
