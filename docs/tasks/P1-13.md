# P1-13 ホーム画面追加バナー

**Phase**: 1
**仕様**: docs/PK-SPEC-P1.md §8.5
**依存**: P1-12
**見積**: 0.5日
**ステータス**: 完了

## 読むべきルール
ui-writing.md §5

## やること
iOS Safari のタブで開いた場合のみ案内する。

## 完了条件
- [x] iOS Safari のタブで開いた場合のみ表示
- [x] 1 回閉じたら 30 日再表示しない
- [x] 必須ではないことを明示

## 作業ログ
2026-08-12 実装。`apps/web/src/lib/mobile/installBanner.ts` /
`apps/web/src/ui/mobile/InstallBanner.tsx`。

- 判定は純粋関数に閉じた（UA・standalone・閉じた時刻・現在時刻を受け取る）。
  画面側で `navigator.userAgent` を読み始めると条件が散らばる。
- iOS の Chrome / Firefox / Edge（CriOS / FxiOS / EdgiOS）は**除く。**
  ホーム画面への追加が Safari の共有メニューにしか無く、案内が迷わせる。
- 閉じた時刻は `localStorage`。読み書きが例外を投げても落ちない
  （プライベートブラウズでは毎回出るだけ）。
- 「追加しなくてもご利用いただけます」を常時併記する（§8.5 MUST）。
- SSR では出さない。最初の `useEffect` で判定する（ちらつかせない）。
