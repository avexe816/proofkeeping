# P2-09 W-07 証跡詳細画面

**Phase**: 2
**仕様**: docs/PK-SPEC-P2.md §12.2-12.3
**依存**: P2-08
**見積**: 1.5日
**ステータス**: 完了（2026-08-13）

## 読むべきルール
（CLAUDE.md のみ）

## やること
タイムラインと整合性確認。

## 完了条件
- [x] タイムラインが時系列で表示される
- [x] 「整合性を確認」で改ざんが検出される

## 作業ログ

### 2026-08-13

**W-06（証跡一覧）と W-07（証跡詳細）を足した。**

```
/app/p/:propertyId/evidence           W-06。1 業務日ぶん。
/app/p/:propertyId/evidence/:taskId   W-07。§12.2 の表示順。
```

API は §14.2 の経路名に合わせた（`routes/api/v1/evidence.ts`）。
P2-08 が置いた `GET /api/v1/tasks/:taskId/evidence/verify` はそのまま残る。

- `packages/engine/src/evidenceTimeline.ts`（17 テスト）が §12.3 の
  タイムライン。**証跡の payload からは組まない**（DECISIONS #077）。
  材料は `taskTimeLog` / `inspection` / `reworkCycle`。
- `lib/evidence/photoIntegrity.ts` が §6.3 の**写真の実体照合。**
  R2 のバイト列から SHA-256 を取り直す（`customMetadata` を信用しない）。
  不一致は `console.error` と監査ログ（`export.data`）へ。
- `lib/evidence/detail.ts` が W-06 / W-07 の読み取り。画面と API が同じ関数を通る。
- 権限は `task.read`（P2-08 の判断を踏襲）。ZIP の持ち出しだけ
  `evidence.export` を新設した（P2-10 と共有）。
- サイドバーの `nav.cleaningRecords` を `PLANNED` → `READY` にして W-06 へ繋いだ。

**整合性の確認は押したときだけ走る。** 写真 20 枚 × 500KB を R2 から読むので、
loader では走らせない（`action` の `intent=verify`）。

--- 入れていないもの（意図的）---
- **§12.2 の「清掃チェックリストと写真」の節が無い**（OPEN_QUESTIONS #049）。
  写真はハッシュとしてのみ照合する。並べると担当者ごとの作業内容を
  一覧する面ができる（§1.3）。
- **§14.2 の `POST /api/v1/evidence/:id/corrections`（§6.4 の訂正）が無い**
  （OPEN_QUESTIONS #050）。DB 側の列は P2-08 が用意済み。
- **`GET /api/v1/evidence` の `roomId` 絞りが無い。** W-06 に客室での
  絞り込みの記述が無いため。
