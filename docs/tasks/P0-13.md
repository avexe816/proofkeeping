# P0-13 テナント越境テスト基盤 ★最優先

**Phase**: 0
**仕様**: docs/PK-SPEC-P0.md §19.4 第3層
**依存**: P0-07, P0-10
**見積**: 1日
**ステータス**: 完了（枠組み。全テーブルのカバーは各 task が足す）

## 読むべきルール
testing.md §2

## やること
```
tests/fixtures/shard-pairs.ts   同一シャードに落ちる組織ペアを事前計算
tests/tenant-isolation/
  _template.spec.ts
  organization.spec.ts
  property.spec.ts
  room.spec.ts
```

## 完了条件
- [ ] 4 パターンのテストが全テーブルにある → **未達。枠組みと 4 表のみ**
- [x] 同一シャード組織ペアでのテストが含まれる
- [x] CI で `pnpm test:isolation` が実行される

## 作業ログ

### 2026-08-12 完了（枠組み）

追加したもの:

| ファイル | 内容 |
|---|---|
| `tests/fixtures/shard-pairs.ts` | 同一シャード（07）に落ちる組織ペア |
| `tests/tenant-isolation/isolation-suite.ts` | 4 パターンを組み立てる共通スイート |
| `tests/tenant-isolation/_template.spec.ts` | 雛形とカバー範囲のレジストリ |
| `tests/tenant-isolation/{organization,property,room}.spec.ts` | 表ごとの呼び出し |
| `tests/tenant-isolation/tsconfig.json` | Workers 型で検査する 4 本目のパス |

- **4 パターンを共通スイートに閉じた。** 表ごとの spec は
  `describeTenantIsolation({ ... })` を 1 回呼ぶだけ。表ごとに写経すると
  「1 件だけ落ちた表」が必ず生まれ、それは緑のまま見つからない。
- 施設の次元を持つ表には第 5 のテスト（担当施設ゼロなら `1 = 0`）が付く。
- **同一シャードのペアは検証付き。** `_template.spec.ts` が `shardIndexOf()` を
  実際に呼び、2 組織が同じシャード（07）に落ちること、対照群がそうでないことを
  検査する。ハッシュ関数を触るとここが落ちる。
- **カバー範囲をレジストリで固定した。** テナント表 15 件のうち spec が無いものは
  `UNCOVERED_TABLES` に理由付きで宣言する。宣言も spec も無い表があると落ちる。
  表を読み書きする task が spec を足してこの行を消す。

**完了条件 1（全テーブル）は未達。** 現時点で越境テストを書けるのは
リポジトリ関数が実在する 4 表（`organization` / `organization_tax_profile` /
`property` / `room`）だけで、残り 11 表は関数が無い。理由は
`UNCOVERED_TABLES` に 1 行ずつ記録した。

**実 D1 での実測もしていない**（P0-02 が未完で D1 は 1 本のみ）。
現在見ているのは発行される SQL。差し替えは `isolation-suite.ts` だけで済む形にしてある。

構成上の判断は docs/DECISIONS.md #025 を参照。
`pnpm test:isolation` が `--dir` の指定ミスで**1 件も拾わないまま
緑になっていた**のを直した（P0-01 から）。
