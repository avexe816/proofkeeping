# P1-08 M-02 本日のタスク ★最重要画面

**Phase**: 1
**仕様**: docs/PK-SPEC-P1.md §9.2
**依存**: P1-05
**見積**: 2日
**ステータス**: 完了

## 読むべきルール
ui-writing.md §3

## やること
清掃スタッフが最も長く見る画面。

## 完了条件
- [x] 「開始する」が 1 タップ（確認ダイアログなし）
- [x] タップ領域 48px 以上、フォント 16px 以上
- [x] 30 秒自動更新＋手動更新＋プルダウン更新
- [x] 並び順が仕様どおり
- [x] オフラインバーが表示される

## 作業ログ
2026-08-12 実装。`apps/web/src/routes/m/today.tsx` /
`packages/engine/src/taskBoard.ts` / `apps/web/src/ui/mobile/useAutoRefresh.ts`。

- 並び順・5 段カウンタは **`packages/engine`** に置いた（node でテストできる）。
  §9.2 の「作業中 → 差戻し → 未着手 → 入室不可 → 完了」。プロトタイプは
  先頭 2 つが逆で、**仕様書を採った**（#035）。
- 「開始する」は 1 タップ。確認ダイアログ無し。押した瞬間に作業中として描き
  （楽観的更新 / §8.3）、送信はキューが持つ。
- 30 秒の自動更新はオフライン時・非表示時に止める。手動更新ボタンは 48px。
  プルダウンは自前で拾う（標準の pull-to-refresh はページごと読み直すため）。
- 一覧の経過は**開始時刻からの時計。** 中断を差し引いた実作業時間は時間ログから
  しか出せず、一覧で引くとタスクの数だけクエリが増える（§13）。正確な値は M-03。
- 施設のグループ表示（§19.3）は **P1-21 の担当**なので入れていない。担当が
  複数施設に跨るときは見出しの施設名を伏せる。
