# P5-03 料金設定

**Phase**: 5
**仕様**: docs/PK-SPEC-P5.md §2.2, §3.2
**依存**: P5-02
**見積**: 1.5日
**ステータス**: 完了

## 読むべきルール
billing.md §8

## やること
5 段階の優先順位で解決する。

## 完了条件
- [x] 5 段階の優先順位で解決される
- [x] 有効期間が判定される

## 作業ログ
- 2026-08-14: 実装。
  - `packages/billing/src/pricing.ts` — `resolvePricingRule()` / `pricingRuleStage()`
    / `isEffectiveOn()`。純粋関数（DB・fetch・`Date.now()` を持ち込まない）。
  - `priority` は**小さいほうが勝つ**（§3.2）。P5-01 の schema コメントが
    逆だったので直した（DECISIONS #122）。
  - §3.2 の梯子に載らない形（施設 + 客室タイプ など）は
    `POST /api/v1/pricing-rules` が 400 で断る（DECISIONS #123）。
  - `PATCH /:id` は **`validTo` しか触らない。** 値上げは行の追加で表す
    （既存行を書き換えると過去の請求書の根拠が変わる）。
  - 正例・負例を 5 件以上ずつ（testing.md §3）。並び順を変えても
    結果が変わらないことを固定した。
