# P5-03 料金設定

**Phase**: 5
**仕様**: docs/PK-SPEC-P5.md §2.2, §3.2
**依存**: P5-02
**見積**: 1.5日
**ステータス**: 完了

## 読むべきルール
billing.md §8

## やること
5 段階の優先順位で解決する。

## 完了条件
- [x] 5 段階の優先順位で解決される
- [x] 有効期間が判定される

## 作業ログ
- 2026-08-14: 実装。
  - `packages/billing/src/pricing.ts` — `resolvePricingRule()` / `pricingRuleStage()`
    / `isEffectiveOn()`。純粋関数（DB・fetch・`Date.now()` を持ち込まない）。
  - `priority` は**小さいほうが勝つ**（§3.2）。P5-01 の schema コメントが
    逆だったので直した（DECISIONS #122）。
  - §3.2 の梯子に載らない形（施設 + 客室タイプ など）は
    `POST /api/v1/pricing-rules` が 400 で断る（DECISIONS #123）。
  - `PATCH /:id` は **`validTo` しか触らない。** 値上げは行の追加で表す
    （既存行を書き換えると過去の請求書の根拠が変わる）。
  - 正例・負例を 5 件以上ずつ（testing.md §3）。並び順を変えても
    結果が変わらないことを固定した。
- 2026-08-14: 画面から料金を編めるようにした。
  - `apps/web/src/routes/app/counterparties.tsx` の `PricingSection`。
    一覧（`stage` 付き）・追加・期間の終了。**書き換えの口は無い。**
  - **選ばれない形を組ませない。** §3.2 の梯子に載らない形（例: 施設 +
    客室タイプ）は API が 400 で断る（DECISIONS #123）が、画面は
    「適用範囲」を 5 段から選ばせ、その段が使わない軸をサーバー側で落とす。
    400 を見せて直させるより、選べない形にするほうが早い。
  - 段 1 の客室タイプは `施設::客室タイプ` の 1 つの選択肢にした。
    施設と客室タイプを別々の欄にすると、施設 A の下に施設 B の客室タイプを
    組み合わせられてしまう。
  - 梯子から外れた行（`stage === null`）は「選ばれません」と表示する。
    登録時に断っているので通常は出ないが、仕様が変わったときに
    古い行を見つけられるようにしておく。
