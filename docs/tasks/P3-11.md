# P3-11 W-20 観察項目の設定

**Phase**: 3
**仕様**: docs/PK-SPEC-P3.md §2.6
**依存**: P3-01
**見積**: 0.5日
**ステータス**: 完了

## 読むべきルール
（CLAUDE.md のみ）

## やること
施設ごとの品目有効・無効。

## 完了条件
- [x] 品目ごとに有効・無効を切り替えられる

## 作業ログ
- 2026-08-13 完了。`/app/settings/observation`（W-20）。品目 19 件の
  有効・無効、入力画面に出す 6 項目、未記録率の警告閾値。
- **REST API を作っていない**（DECISIONS #099）。§7 に設定の口が無いため、
  画面の action から `upsertObservationConfig()` を呼ぶ（W-17 と同じ形）。
- `enabledItemCodes` の既定は空。**空を「全品目」と読み替えない**
  （§2.5 MUST。未設定の施設に 19 品目が並ぶと §1.2 の 15 秒が壊れる）。
