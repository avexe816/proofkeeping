# P7-16 RUNBOOK

**Phase**: 7
**仕様**: docs/PK-SPEC-P7.md §7.2
**依存**: P7-14
**見積**: 2日
**ステータス**: 文書は完成（2026-08-15）／実地の確認のみ人間待ち

## 読むべきルール
（CLAUDE.md のみ）

## やること
社内向け運用文書。

## 完了条件
- [x] システム概要書が電子帳簿保存法の備付け要件を満たす
- [ ] 障害対応手順で実際に対応できた ⏳ **人間待ち**（P7-14 と同時に確かめる）

## 作業ログ

### 2026-08-15

**依存の P7-14（復旧訓練）は「人間が実施」だが、手順書が無ければ
訓練できない**（P7-14 の完了条件が「手順書だけで対応できた」）。
文書を先に書き、実地の確認だけを人間に残した（DECISIONS #175）。

`docs/runbook/` に §7.2 の 7 文書を置いた。

| 文書 | 実体 |
|---|---|
| 索引 | `README.md` |
| システム概要書 | `system-overview.md`（**電子帳簿保存法の備付け要件**） |
| アーキテクチャ図 | `architecture.md` |
| 障害対応手順 | `incident-response.md`（**症状別**） |
| シャード移送手順 | `shard-move.md` |
| 復旧手順 | `recovery.md`（D1 Time Travel / R2 バージョニング） |
| デプロイ手順 | `deploy.md`（16 シャードのマイグレーション） |
| オンコール体制 | `oncall.md` |

#### 電子帳簿保存法の備付け要件（完了条件 1）

求められるのは 4 種（システム概要書 / システム仕様書 / 操作説明書 /
事務処理マニュアル）。**4 種すべての所在を対応表から辿れる形**にし、
`tests/docs/runbook.spec.ts` が**参照先の実在**まで確かめる。

事務処理マニュアルは本書 §5 として書いた（入力・訂正・保存期間・障害時）。

#### 手順書が実装より先に進まないようにした

- `deploy.md` §7 に**まだ無いもの**の一覧を置いた（production の D1・
  キュー・R2 バケット・和文フォント・エラー集約・503 middleware）。
  spec がこの節の存在を固定する。
- `oncall.md` の連絡先は**枠だけ**。実在しない名前と電話番号を書かない。
  **GA 判定の前に人が埋める。**
- `shard-move.md` §4 に「実機での確認は未実施」と明記した。
- 参照する `pnpm` script が実在することを spec が package.json と突き合わせる。

#### 完了条件 2（障害対応手順で実際に対応できた）

**書いただけでは満たせない。** P7-14（復旧訓練）で、
この文書を読んだだけの担当者が質問なしで完了できるかを見る。
詰まった箇所はその場で追記する。訓練の記録は `recovery.md` §5。

#### 止まらずに判断したこと

- DECISIONS #175 RUNBOOK は P7-14 より先に書く
