# P1-15 W-03 客室ボード / M-10

**Phase**: 1
**仕様**: docs/PK-SPEC-P1.md §9.5
**依存**: P1-05
**見積**: 1.5日
**ステータス**: 完了

## 読むべきルール
（CLAUDE.md のみ）

## やること
フロア別の客室ステータス表示。

## 完了条件
- [x] 4 状態が色分けされる
- [x] タップで担当者・経過時間・写真枚数が見える

## 作業ログ

### 2026-08-12 実装

- `packages/engine/src/roomBoard.ts` … 階でまとめ、客室番号順に並べる（純粋関数）
- `apps/web/src/lib/room/board.ts` … 読み出し（客室・階・タスク・写真枚数・スタッフ）
- `apps/web/src/ui/RoomBoard.tsx` … 盤面の描画。**W-03 と M-10 で共有**
- `apps/web/src/routes/app/propertyBoard.tsx` … W-03（P0-21 の器に中身を入れた）
- `apps/web/src/routes/m/board.tsx` … M-10

4 状態の色は `styles/app.css` の `.pk-room--*`。**入室不可は青**
（契約 §11.2「保留（入室不可）を赤で表示 → 青」）。

タップで担当者・経過時間・写真枚数を出す（§9.5）。**操作履歴は出さない**
（INV-07）。担当者名は INV-06 の出し分けを通る。

写真枚数は `countPhotosByTask()` で 1 回に引く。**タスクごとに引かない**
（100 室で 100 クエリになる / §13）。

