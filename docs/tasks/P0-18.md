# P0-18 seed データ

**Phase**: 0
**仕様**: docs/PK-SPEC-P0.md §11
**依存**: P0-06
**見積**: 0.5日
**ステータス**: 完了（実行経路は未配線）

## 読むべきルール
（CLAUDE.md のみ）

## やること
3 施設 120 室、清掃スタッフ 15 名を投入する。

## 完了条件
- [x] 3 施設 120 室、清掃スタッフ 15 名が投入される
- [x] 3 回連続実行しても同じ状態になる
- [x] production では実行できない

## 作業ログ

### 2026-08-12
- 完了条件 3 件すべて達成（**実行経路を除く。下記**）。
- `packages/db/src/seed.ts`。3 施設 60/40/20 = 120 室（§23.2 の例と同じ）、
  清掃スタッフ 15 名、清掃専用の場所 3 室（客室数に数えない / §24.3）。
- **PIN は必ず `pinSchema` を通してから `hashPin()` へ渡す。**
  `hashPin()` を直に呼ぶと連番・ゾロ目を登録できる（P0-09 の申し送り）。
  ハッシュ化の実装は `apps/web` にあるため注入で受ける
  （`packages/*` から `apps/*` は import できない）。PBKDF2 を書き直さない。
- 冪等性は「決定的な ID（固定時刻＋カウンタの ULID）」と
  「全 INSERT の `onConflictDoNothing()`」の**両方**で担保。片方では
  2 回目に落ちるか行が増える。3 回実行して同じ SQL になることを spec で検証。
- production は `ENVIRONMENT` を見て実行を止める。上書きの余地を作らない。
- インボイス登録番号は未設定のまま入れる。P0-16 の「発行できません」表示を
  シードのまま確かめられるようにするため。
- **task に無いが入れたもの: 管理者 1 名。** 無いと画面に入れず、投入した
  3 施設 120 室を確かめられない。パスワードは呼び出し側が渡す（既定値を持たない）。
- **未達: `pnpm db:seed` を配線していない。** OPEN_QUESTIONS #031 を参照。
