# P3-07 観察記録の事後修正

**Phase**: 3
**仕様**: docs/PK-SPEC-P3.md §2.2
**依存**: P3-01
**見積**: 0.5日
**ステータス**: 完了

## 読むべきルール
security.md §6

## やること
PROPERTY_MANAGER 以上のみ。理由必須。

## 完了条件
- [x] `PROPERTY_MANAGER` 以上のみ
- [x] 理由が必須
- [x] `observationRevision` に旧値が残る

## 作業ログ
- 2026-08-13 完了。`PATCH /api/v1/observations/:observationId`。
  `observation.amend`（`PROPERTY_MANAGER` 以上）・理由必須・旧値を
  `observationRevision` へ積む・`recordAudit("observation.amended")` の 4 つ。
- **`usedDefaults` / `inputDurationMs` は書き換えない。** どちらも
  「現場が入力したときの事実」で、動かすと W-22 の入力品質が別のものを指す。
- 画面はまだ無い（W-19 は P3-12 の範囲外／§6.1 に一覧としてのみ定義）。
  API と権限までがこの task。
