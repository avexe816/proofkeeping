# P1-10 M-04 チェックリスト

**Phase**: 1
**仕様**: docs/PK-SPEC-P1.md §9.4
**依存**: P1-06, P1-09
**見積**: 1日
**ステータス**: 完了

## 読むべきルール
ui-writing.md §3

## やること
セクション折りたたみ式のチェックリスト。

## 完了条件
- [x] **「すべてチェック」ボタンがない**
- [x] チェック時刻が個別に記録される
- [x] 完了セクションが自動で折りたたまれる

## 作業ログ
2026-08-12 実装。`apps/web/src/routes/m/checklist.tsx`。

- 3 値（できた / できなかった / 該当なし）で 1 項目ずつ記録する（INV-22）。
  一括更新のボタンも API も無い。
- **`POST /tasks/:taskId/checklist` が `/:taskId/:action` に吸われていた。**
  Hono は登録順に照合し静的な区間を優先しないため、`action = "checklist"` として
  弾かれ 404 になっていた（P1-06 の配線）。登録順を入れ替えて直し、
  `tasks.spec.ts` に回帰テストを置いた。**この修正が無いと M-04 は動かない。**
- 完了したセクションは自動で折りたたむ。手で開き直した場合はそちらを優先する。
- 記録は手元に即反映し、送信はキューへ（機内モードでも 16 項目を記録できる）。
