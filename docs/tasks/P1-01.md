# P1-01 スキーマ: 清掃タスク

**Phase**: 1
**仕様**: docs/PK-SPEC-P1.md §2.1
**依存**: P0 完了
**見積**: 1日
**ステータス**: 完了

## 読むべきルール
architecture.md

## やること
`cleaningTask`, `taskTimeLog`, `checklistTemplate`, `checklistItem`,
`taskChecklistResult`, `taskPhoto`, `standardTime`, `dailyRoomPlan`

## 完了条件
- [ ] `@@unique([roomId, businessDate, taskType])` がある
- [ ] `shortId` が 8 桁で一意
- [ ] 越境テストが全テーブルにある

## 作業ログ

### 2026-08-12 — 完了

**追加した 8 表**（`packages/db/src/schema/task.ts` / `checklist.ts`）

`cleaningTask` / `taskTimeLog` / `taskPhoto` / `standardTime` / `dailyRoomPlan` /
`checklistTemplate` / `checklistItem` / `taskChecklistResult`。
migration は `0003_p1_01_cleaning_task.sql`（全 CREATE TABLE + `property` への
`inspection_required` 追加。後方互換）。

**完了条件**

- [x] `@@unique([roomId, businessDate, taskType])` がある
      → `uq_cleaning_task_room_date_type`。**先頭に `organization_id` を足してある**
        （`schema.spec.ts` が「全 index が organization_id から始まる」を固定するため）。
        組織を跨いだ重複は `roomId` が自己記述 ID なので起こり得ない。
- [x] `shortId` が 8 桁で一意 → `uq_cleaning_task_short_id`（組織内一意 / DECISIONS #033）。
      採番は `apps/web/src/lib/task/shortId.ts`。既存を避けてから採る。
- [x] 越境テストが全テーブルにある → `tests/tenant-isolation/` に 3 ファイル追加。
      `_template.spec.ts` の `TENANT_TABLES` に 8 表を登録済み。

**判断したこと**

1. **enum の語彙は PK-SPEC-P1 §2.1 を採った。** 実装契約書 §2.1 と食い違うが、
   契約書の 6 状態では §5.1 の状態機械（9 状態）を表現できない。
   一方チェックリストの実施結果は**契約書の 3 値**（INV-22 が不変条件として明示）。
   → DECISIONS #031 / OPEN_QUESTIONS #032。
2. `property.inspectionRequired` を 1 列足した。§5.2 が名指しする列で、
   これが無いと P1-05 の `complete` が行き先を決められない。既定 `false`
   （既定が真だと、検査画面の無い P1 で全タスクが滞留する）。
3. `sequenceInDay`（§19.5）を初回の CREATE TABLE に含めた。`room` の 3 カラムと同じ判断。
4. **`room.housekeepingStatus` を作っていない**（§11.1）。task の やること 8 表に無く、
   客室ステータス同期は P1-16 の担当。→ OPEN_QUESTIONS #034。
5. **`dailyRoute`（§19.5）を作っていない。** P1-21〜23 の担当。

