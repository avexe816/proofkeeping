# P2-11 忘れ物管理

**Phase**: 2
**仕様**: docs/PK-SPEC-P2.md §7
**依存**: P2-01
**見積**: 2日
**ステータス**: 完了（2026-08-13）

## 読むべきルール
security.md §3

## やること
LostItem と期限管理。

## 完了条件
- [x] 管理番号が重複なく採番される
- [x] **期限到来でも自動廃棄されない**
- [x] `CLEANER` に保管場所・返却先が見えない
- [x] 所有者の氏名・連絡先を保存していない

## 作業ログ

### 2026-08-13

`schema/report.ts` に 3 表（`lostItem` / `lostItemPhoto` / `lostItemHistory`）。
migration は `0009_p2_11_12_lost_item_and_issue.sql`（P2-12 と共通）。

**完了条件の満たし方**

| 条件 | どこで守っているか |
|---|---|
| 管理番号が重複なく採番される | `UNIQUE(organizationId, propertyId, managementNo)` ＋ 衝突時に 3 回まで採り直す（`lib/report/lostItem.ts`）。`DocumentSequencer` を使わない理由はリポジトリ冒頭 |
| **期限到来でも自動廃棄されない** | engine に**期限から状態を導く関数が 1 つも無い**。`warningLevelFor()` は色を返すだけ。Cron / Queue から呼ぶ経路も無い |
| `CLEANER` に保管場所・返却先が見えない | `toLostItemSummary()` が `lostItem.readStorage`（security.md §1 の絶対境界）へ問い、`storageLocation` と `policeReportNo` を `null` にする。一覧も `foundById = 自分` で絞る |
| 所有者の氏名・連絡先を保存していない | 列も引数もスキーマも無い。連絡は `POST /:id/owner-contacted`（**本文を取らない**）で時刻だけ記録 |

engine `lostItemRules.ts`（23 テスト）が §7.2 の管理番号と §7.3 の期限・警告。
API は §14.3 の経路名。越境テストは 3 表ぶん（`tests/tenant-isolation/report.spec.ts`）。

--- 入れていないもの（意図的）---
- **写真の受け口が無い**（OPEN_QUESTIONS #051）。表と `createLostItemPhoto()` はある。
  §7.5 の「1 枚必須」は効いていない（`PHOTO_REQUIRED` は用意済み）。
- **施設ごとの保持日数の列が無い**（OPEN_QUESTIONS #052）。常に engine の既定。
- **W-09 忘れ物管理画面が無い。** §12.1 の一覧で、P2-11 の完了条件には無い。
