# P2-03 InspectionLock（Durable Object）

**Phase**: 2
**仕様**: docs/PK-SPEC-P2.md §19.9(P0)
**依存**: P2-01
**見積**: 0.5日
**ステータス**: 完了（2026-08-13）

## 読むべきルール
architecture.md §4

## やること
検査開始の排他制御。

## 完了条件
- [x] 同時検査開始で 1 件のみ成功する

## 作業ログ

2026-08-13。

- `apps/web/src/durable/InspectionLock.ts`（新規）。
  `InspectionGate`（本体・DO の外でも動く）と DO の殻。
  経路は `/acquire` `/release` `/peek` の 3 本。
- binding `INSPECTION_LOCK` を wrangler.toml の 4 環境すべてへ。
  migrations タグ `v2` を追記（**既存の `v1` は書き換えない**）。
- `packages/db/src/env.ts` と `apps/web/src/index.ts` の export。

**ラウンド番号を保持する**（DECISIONS #063）。差戻し後の再検査で
同じタスクを何度も検査するため、真偽値のロックでは 2 回目が始められない。

50 並列で成功が 1 件だけになることを検査済み（testing.md §5）。
**同じ検査者からの 50 並列は全部通る**（再送は競合ではない）。

**`inspection` の一意制約を外さないこと。** DO は速い断り方であって
唯一の防波堤ではない。DO の呼び出しに失敗した経路でも重複は作らせない。

