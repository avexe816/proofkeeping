# P2-04 検査 API

**Phase**: 2
**仕様**: docs/PK-SPEC-P2.md §4.2-4.5, §14.1
**依存**: P2-03
**見積**: 1.5日
**ステータス**: 完了（2026-08-13）

## 読むべきルール
security.md §1

## やること
検査開始・項目入力・PASS/FAIL。

## 完了条件
- [x] 清掃担当者本人が自分のタスクを検査できない
- [x] 自己検査の例外に理由と監査ログが必須
- [x] 1 項目でも FAIL があれば全体が FAIL
- [x] FAIL に理由コード・コメント・写真がないと完了できない

## 作業ログ

### 2026-08-13

**作った経路**

```
POST /api/v1/tasks/:taskId/inspection/start   §4.2（経路名は §14.1）
GET  /api/v1/inspections/:inspectionId        M-09 が読む
PUT  /api/v1/inspections/:inspectionId/items  §4.3。**1 項目ずつ**
POST /api/v1/inspections/:inspectionId/photos §4.3。multipart
POST /api/v1/inspections/:inspectionId/complete §4.4 / §4.5
```

**完了条件の担保**

| 条件 | どこで | 検証 |
|---|---|---|
| 本人が自分を検査できない | `evaluateSelfInspection()`（engine） | inspectionResult.spec / inspections.spec |
| 例外に理由と監査ログ | 同上 + `inspection.selfApproved`（監査） | inspections.spec「監査ログが残る」 |
| 1 項目でも FAIL で全体 FAIL | `aggregateResult()`。**引数は項目の並びだけ** | 同 spec「ボディで判定を上書きできない」 |
| FAIL の 3 点が無いと完了不可 | `checkInspectionCompletion()` | 同 spec 3 件 |

**止まらずに判断したこと**（DECISIONS #064〜#067）
- 項目更新のボディを配列にしない（#064）
- 項目・完了に `Idempotency-Key` の記録表を作らない（#065）
- 「1 トランザクション」を条件付き更新の連鎖で代替（#066）
- 差戻しサイクルの**生成**までを P2-04 に含めた（#067）

**この task で入れていないもの（意図的）**
- `GET /inspections/waiting`（検査待ち一覧）… P2-05
- 検査の画面（M-08 / M-09）… P2-05 / P2-06
- `POST /reworks/:id/{start,complete,waive}` と M-12 … P2-07
- `EvidenceSnapshot` の生成 … P2-08
