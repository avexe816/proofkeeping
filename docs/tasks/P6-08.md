# P6-08 スタッフキー除外と R002 検証

**Phase**: 6
**仕様**: docs/PK-SPEC-P6.md §4.3-4.4
**依存**: P6-04
**見積**: 1.5日
**ステータス**: 完了

## 読むべきルール
（CLAUDE.md のみ）

## やること
清掃時刻による除外を既定にする。

## 完了条件
- [ ] 清掃タスクの前後 10 分の解錠が除外される
- [ ] `actorType` 不明時に confidence が 25 下がる
- [ ] **不明を推測で埋めていない**

## 作業ログ

- 2026-08-14 完了。
  - `packages/engine/src/reconciliation/staffKey.ts`（純粋関数）。
    R002 と R013 が共有する。**方法 2（清掃タスクの start / complete の
    前後 10 分による除外）を既定にした**（§4.4 MUST）。方法 1
    （`STAFF_KEY` / `MASTER_KEY`）も併せて掛ける。
  - `TaskFact.startedAt` を足した。**`completedAt` から逆算しない**
    （実作業時間は客室ごとに違い、逆算は推測になる）。
  - R002 / R013 が `actorType` 不明の解錠を**数に入れる**ようになった。
    数えない実装だと、鍵の種別を返さない機種でルールが一度も立たない。
    §4.3 が減点（−25）を定めているのは「不明でも立てたうえで弱く出す」
    ことを求めているため。**不明を `GUEST_KEY` に書き換えていない。**
  - 差異の根拠に `actorTypeUnknown` を残し、W-07 が
    「鍵の種別は取得できていません」を出す（§4.3 MUST）。
  - `RECONCILIATION_ENGINE_VERSION` を `1.1` → `1.2`。**判定が変わった。**

### 完了条件の確認
- 清掃タスクの前後 10 分の解錠が除外される → `staffKey.spec.ts` / R002 / R013。
- `actorType` 不明時に confidence が 25 下がる → R002 / R013 の spec。
  **件数に比例させない**（4 件で 0 になると、根拠が増えるほど弱くなる）。
- **不明を推測で埋めていない** → 根拠の `signals[].actorType` が `null` の
  まま残ることを spec で押さえた。

### 止まらずに判断したこと
- 減点で `matchedSignals` を増やさない。あの件数は §1.3 の
  「単一シグナルで 80 以上を出さない」を解く鍵で、増やすと
  **不明であることが確信度の上限を上げる**という逆立ちが起きる。

### 残したもの
- **R002 / R013 の実データ検証（§8.2 の「R002 / R013 が実データで動作する」）
  は人間が実施。** スマートロックの実接続がまだ無い（§11 の未決事項 2）。
